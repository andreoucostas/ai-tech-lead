# RK1 private-network checkpoint

User authorized proceeding after the Sonnet diagnostics. The original preparation deadline has
passed; this prospectively authorizes only a 30-minute network-mechanism checkpoint from 09:47
London on 2026-09-09, not a sample/budget reset or study dispatch. The existing six-hour envelope
and credit limits remain; earlier timed-out Sonnet calls conservatively reserve their USD 4 caps.
Opus contract review adds reported USD 0.0946025 within a USD 1 allocation.

The frozen external contract is `C:/TEMP/rk1-private-20260909/contract.md`, including root's adopted
Opus corrections. Its question is finite peer/egress feasibility, not full selective transport.
Root uses individually checked Docker commands and two synthetic TCP scripts, avoiding the
previous draft's blanket cleanup. One fixed private network, pinned toolchain image, Hyper-V,
ContainerUser, no mounts/credentials/source; each successfully created ID must match its unique
ownership label before removal. Native failures stop forward execution. Host state is captured
before/after; expected Docker/HNS state is distinguished from direct host reconfiguration.

Require exact peer nonce, closed-port refusal and classified provider-IP failure in one actor
lifetime, bracketed by NAT success to that same literal IP/port. Other destinations/protocols are
unproved. On success, a separately reviewed private+NAT proxy may follow; no hostname-only claim
of path filtering and no model/efficacy claim follows from this checkpoint. Preserve all partial
results, review gaps and cleanup evidence in the existing RK1 packet/backlog/decision record.

## Observed checkpoint

The first network carried the exact peer nonce and the actor's provider-IP connection timed out,
bracketed by NAT success. Its closed peer port timed out rather than refusing; the original full
conjunction remains unmet. The peer's loopback closed port did refuse. Do not backfill a pass.

A separately reviewed second contract (`relay-contract.md` in the same external directory)
authorized a finite fixed-HEAD relay observation within the same checkpoint. Opus accepted it,
requiring measured forwarding flags and the original deadline; reported review cost USD 0.089496.
The NAT-created proxy accepted a private endpoint before first start. The actor stayed private-only.
Fixed HEAD returned the expected nonce/status 404; OTHER returned DENY; direct actor TCP to the
frozen provider IP timed out while proxy direct connections succeeded before/after. PS7 and PS5.1
clients both observed allowed/denied responses. Relay logs bind exactly two outbound events to the
two allowed requests and none to the two denied request IDs. Guest forwarding was observed disabled.

Both networks and all five created containers were removed after ID/label checks. Final captured
network/container identities, adapter fields, addresses/routes and firewall-rule fields matched
the initial baseline. This is not proof about unobserved host state. Frozen external evidence
manifest SHA-256: `822EF467C9711B327E13B2DDA7C495FE853E58F97144E7F8B573584BD6D5E314`.
Authenticated native Copilot transport, comprehensive access controls and study dispatch remain
unrun; the fixed request relay is not a production proxy or substitute model executor.

Post-execution code/receipt review returned ACCEPT NARROW CODE/RECEIPTS from explicit Opus 5,
reported USD 0.1296895. A preceding broad review exhausted its requested USD 0.50 flag without a
verdict, reporting USD 0.710734 and usage under Opus 5 plus Opus 4.8; it is not accepted review
evidence. The fresh completion prospectively reserved USD 0.30 from the original USD 10 campaign
allowance. Total reported review cost for this checkpoint is USD 1.024522; no invoice claim follows.
Root accepts the narrow code/receipt verdict, not the reviewer's stronger implication that two
bracketing successes exclude all transient failures. Client-host identity rests on root's exact
invocations; the provider literal and relay hostname need not resolve to the same address.
