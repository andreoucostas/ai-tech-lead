# RK1 execution campaign — revision 1

**Authority:** user request on 2026-09-09 to execute RK1, with Claude CLI Opus review or Sonnet
implementation where useful. This authorizes preparation and the bounded study below. It does not
reopen CP1 or CP2. Evidence is a maintainer component comparison; independent FS2 remains separate.
**Initial stop:** preparation stopped at the required native sandbox policy on 2026-09-09; no study
task, setup, or Copilot model calibration ran. This is a retained blocker, not campaign retirement.
The checkpoint and resumption requirements are in `meta/repository-knowledge-component-study.md`.
**Subsequent state:** user-authorized mechanism checkpoints demonstrated an outer private network
and one synthetic native Copilot HTTP-fallback response through relay-mediated credentials. The
second-request, comprehensive access/observer, treatment and oracle obligations remain unproved;
no setup/task arm has run. The later checkpoint plans preserve the original envelope and budgets.

## Question, treatment, and decision

Measure the marginal value of the released broad discovery/capture/use increment above the same
framework with recurrence-oriented discovery and its other existing knowledge paths retained.
ENHANCED is v0.86.0, peeled commit `458a298ad4a876445c8e2ec5c0ea411435a39cf3`.
CONTROL will be a separately committed experimental source ablation of that release. It is not a
released version. Both source commits and composed artifact hashes must be frozen before setup.
This prospectively resolves the original packet's pre-release CURRENT/ENHANCED wording. The
original protocol remains preserved; this revision supplies its concrete campaign choices.

Keep B-216 operation authority/project-pattern references, B-226 review scope, B-227 financial
evidence, B-230 archive preservation, B-231 bug-fix scope, native Copilot delegation/sequential
fallback and current adjacent wording in both arms. The source-history audit found overlapping
edits, so a whole-file rollback cannot establish this contrast. List all remaining CONTROL
knowledge paths explicitly, including manually invoked wiki capture, existing navigation, and
project-pattern support for evidenced operations. A null is a bounded marginal-value result above
that residual; it cannot establish that repository knowledge has no value.

Independent review must inspect every installed-byte difference and map it to the declared
treatment. Freeze a path/hash manifest and source hunks; preserved whole files compare by raw hash,
and preserved sections in changed files compare exactly. Plant one unrelated removal in a separate
review copy, observe refusal, restore, and review the actual frozen clean artifacts. This targeted
control does not certify semantic purity or justify a new generic gate. Existing product validators
must pass for installed inputs before dispatch; separately identify feature-presence assertions
that intentionally discriminate the experimental control. Do not call those assertions green.

The concrete uncertainty is whether the shipped increment repays its setup, refresh, context and
review cost on ordinary coding tasks. Further instruction expansion is unsupported by recent
acceptance observations. A read-only evidence summary cannot answer this paired question; a
whole-release comparison would mix unrelated repairs. Reuse current launchers and controls, with
only the small missing host adapter if required by an actual observation.

## Selection and task contract

Reuse the public application and immutable release anchor from CP1 because its inventories,
dependencies and baseline assets are available. Freeze the same newest-first prefix of 100
first-parent integrations from its retained 238-integration, 90-day population as an explicit
selection-cost bound, before new eligibility screening. Reacquiring absent public Git history
does not extend this prefix. No candidate 101, substituted repository or enlarged window follows
failure. CP1 verdicts and its three-decision/file-count requirements do not govern RK1.

Select the first eligible task in each previously unrepresented repository area, stopping at four.
Each needs a meaningful requested change, at least one consequential nonlocal decision grounded in
pre-change evidence, a passing application baseline, executable private acceptance and supported
alternatives. Exclude generated-only, dependency-only and security-sensitive changes. Preserve
the complete actual pre-change repository for each pair. Four different snapshots are permitted;
setup then runs independently eight times. Source population, exact revisions, requests, grading
keys, prior exclusions and solutions stay in coordinator storage outside every actor scope.

Before setup, independently confirm that the four selected tasks collectively exercise quiet
unique, helper-derived, reusable-operation and conflicting-scope knowledge. If the first eligible
four do not meet this coverage, record insufficient coverage and stop; do not replace them after
seeing outcomes or enlarge the sample within this budget. Prior maintainer inspection and public
model familiarity must appear in each result row's limitations.

Freeze each task's prompt, exact baseline/verification commands, decision evidence and hard/soft
labels, supported alternatives, source-bound hidden grader, severe errors and question-response
card. Observe baseline, valid solution, supported alternatives and each targeted decision violation
before either setup receives the repository. Zero discovered tests is cannot-examine. Keep all
task answers and future-change instructions out of setup; preserve generated artifacts as produced.

## Hosts, stages, and bounds

Use Windows Copilot CLI 1.0.83, exact `gpt-5.4` model route, medium effort, fresh sessions with
memory/remote export/auto-update disabled. Do not fall back to Auto or a different model. VS Code
is unrun. Reviewer/implementation Claude calls cannot substitute for Copilot study outcomes.

Coordinator-selected ceilings are 1,600 Copilot credits: 160 total calibration, eight setups at
80 each, eight task arms at 100 each. Confirm available allowance before paid Copilot work.
Retain the full four-pair allocation before dispatch; an inadequate balance means NOT RUN, not
post-result sample reduction. No subscription purchase or overage-setting change is authorized.
Claude implementation/review calls have a combined USD 10 list-price ceiling; initial Opus review
has a USD 2 ceiling. Report list-price and CLI usage as such, not as invoice observations.

Maximum six hours of campaign work, counted from a coordinator-selected conservative anchor of
07:39 London on 2026-09-09, including preparation and reviews; preparation deadline is 09:39.
Every model session also has
an explicit wall-clock timeout and remaining-budget check before launch. At the preparation
deadline, proceed only if all eight application/setup inputs, both reviewed variants, all task
oracles, and RK1-C1–C10 prerequisite observations are ready. Otherwise stop with retained evidence.
Ceilings allow a partial outcome; they do not promise the work fits.

First establish host feasibility before expensive task/oracle construction. Required host evidence:
an allowed canary read and all excluded canary denials under the same actual mechanism/identity;
no shell bypass; a neutral one-commit/no-remote root; clean answer-token preflight; and permitted
model transport in that same configuration. Native file reads/edits and shell execution require
separate canaries. If both allowed and denied reads fail, classify an unusable instrument/route,
not successful isolation. Local capability queries are preparation, not execution proof.

An existing bounded sandbox route may be calibrated. If execution requires an OS upgrade, feature
enablement, broad host ACL changes or permanent host tooling, stop and identify that prerequisite.
The old private-network proposal is not a ready substitute. Never use a CLI path allowlist as
proof that arbitrary shell execution cannot read coordinator/history/other-arm/oracle/result files.

Before task dispatch, observe RK1-C7 positive actual content/tool access and its negative echo-only
control on the exact host. Observe all applicable RK1-C1–C10 controls with their original intended
passing and rejecting states. Freeze the launcher argv/policy and retain partial typed output on
timeout. Confirm the route, no answer leaks, actual installed validators and application baseline
before setup and before each task. Do not repair setup harm or semantic misses into acceptance.

## Scoring and handoff

Four pairs use fixed alternating arm order: CONTROL first for pairs 1/3, ENHANCED first for 2/4.
Use identical prompts and equal caps per pair. Freeze outputs before scoring or repair. Only a
requested task-plan approval gets the predetermined continuation; other questions use the frozen
response card. Record interventions, rather than silently steering an arm.

Report executable acceptance, each nonlocal decision, severe errors and acceptability (0/1/2)
separately. A pass/fail difference on acceptance or a hard decision, an acceptability difference,
or a severe error is material. Separate burden signal: at least 15% and five minutes of active
human review/rework, or two human interventions. Agent/coordinator elapsed time is not human effort;
unavailable effort stays unavailable. Record setup/refresh, loaded context, elapsed time and usage
separately. Preserve supported alternatives, nulls, regressions, void pairs and inaccessible evidence.

Sanitized campaign state/results belong in the existing RK1 packet, backlog and decision log;
append the learning/RCA. Raw source, task keys, streams and grader outputs remain external.
Meta-only records need no product release. Complete relevant native PS7/PS5.1 document checks,
commit and push through the repository wrapper, and report CI evidence separately from study results.

## Independent pre-lock critique and adjudication

Initial read-only Claude Opus 5 xhigh review returned REVISE after reading the original protocol
and root guidance before the proposal. Its reported list-price cost was USD 0.473566. Accepted
corrections: explicit arm amendment, detectable out-of-scope ablation, residual CONTROL knowledge,
observable readiness, positive/negative host controls, full allocation before task dispatch and
frozen sample stopping rules. A CONTROL source commit is feasible, so artifact-only provenance
is unnecessary. Residual knowledge limits interpretation but does not itself make a null invalid.

The review correctly rejected a build-table-only impossibility claim. Subsequent direct root
query of the current OS sandbox API returned success and capability mask 7 (create, deny paths,
proxy). Root independently observed installed Copilot runtime support true, then replayed its
refusal of the required policy before the own-canary command ran. Capability detection does not
establish execution. Concrete variant, oracle and host evidence still require independent review.

A fresh read-only Opus 5 xhigh check accepted revision 1 for bounded preparation and variant
construction, at reported USD 0.5104405. Both reviewers had no host/application execution access.
Their acceptance is design evidence. The second review's broad phrase about checks before any
paid dispatch is interpreted against the actual staged contract: bounded model calibration follows
its non-model controls; completed variants/task oracles precede setup and task arms. An observer
cannot be required to be calibrated before the call that calibrates it.

## Preparation stop

Two offline CLI sessions with minimal and expanded Windows environments refused the required
policy. Root's separate installed-SDK shell replay returned platform support true, shell success
false, empty command output and exit 3. The emitted policy added system-volume read access despite
empty saved read-only grants; removing the five path denials would expose excluded study material.
The emitted network policy denied ingress/egress despite a conflicting UI label. Neither network
leakage nor universal Windows impossibility was established. Both stable-release checks returned
the installed CLI version.

Four provisional source cards and earlier exclusions were retained; root verified the population
order and candidates' parent/path sets. No application baseline, independent oracle/coverage
acceptance, CONTROL implementation, setup or task followed. One proposed task's test configuration
does not include its relevant specs. The stop preceded the preparation deadline and any Copilot
model call; two Claude reviews reported USD 0.9840065 combined list-price cost.

Resume after one configuration demonstrates permitted model transport, successful allowed access
and rejected excluded access, with native file tools separately calibrated. A policy-compatible
Windows runtime or an isolated Windows guest with established transport can meet that contract;
the retained network-none container does not establish it. Preserve the sample, evidence and
consumed review cost, and check remaining time/credit bounds prospectively. No host upgrade, ACL
change or product release was performed.
