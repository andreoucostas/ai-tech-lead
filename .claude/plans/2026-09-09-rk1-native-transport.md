# RK1 native transport checkpoint

User authorized continuing after the fixed-HEAD relay. The external frozen contract is
`C:/TEMP/rk1-transport-20260909/contract.md`, including root's pre-implementation disposition of
Opus critique (REVISE, reported USD 0.078257). The checkpoint spans 10:32–11:17 London on
2026-09-09 within the original six-hour envelope; it does not reset sample/budgets or authorize
study dispatch. Additional Claude allocation USD 1.50 with overshoot headroom; prior reported
costs plus full timeout reservations USD 6.0085285. Sonnet receives only a bounded relay
implementation task; root owns lifecycle commands, hostile controls and observed adjudication.

Candidate: native Copilot 1.0.83 CAPI URL override/direct API authentication and documented HTTP
Responses fallback, private-only actor, dual-homed fixed-upstream relay. No BYOK or custom tool
bridge. Real credentials remain in trusted relay memory, never actor storage/arguments/logs.
Allow only model metadata and one synthetic gpt-5.4 medium request, with output capped at 256
tokens as an explicit calibration-only payload change. Native full-fidelity study transport,
WebSocket transport, comprehensive access/observer controls and task execution remain unproved.

Before forwarding: independent critique, strict framing/path/body rejects and positive metadata
control, actual native metadata discovery, direct actor failure with NAT-positive bracket.
Freeze fresh nonce before one model call; require complete nonce plus provider/native receipts.
Review code/receipts and remove exact owned IDs before recording a narrow result. Never disable
audit logging; an enabled token-logging policy is a stop condition. Preserve partial failures.

## Observed disposition

Sonnet timed out at 180 seconds before writing a file; reserve its full USD 0.70 requested cap,
not an observed charge. Root implemented the bounded external scripts. Direct API-token auth
required the alternate-provider flag but could not select a supported native model. The separately
reviewed `oauth-amendment.md` (Opus REVISE, root disposition adopted; USD 0.089203 reported)
used normal-token auth shape under explicit API URL overrides, real credentials injected only by
the fixed relay. No exchange token or repository/history path was forwarded.

The installed CLI rejected cap5 (minimum30) and retained tools for omitted/empty allowlists.
Corrected preflight used an explicit nonmatching tool name: native /responses HTTP, gpt-5.4,
medium, non-BYOK/non-Auto, tools0; relay denied it while the trusted permit flag was absent.
After the recorded root decision, exactly one upstream POST returned HTTP200 and the complete
fresh nonce, native exit0. The 256-token output clamp is a declared payload mutation, not study
transport fidelity. Ten hostile cases and metadata success ran on both client hosts. The later
second-request probe missed the five-minute service lifetime: cannot-examine, not a pass. No
service restart occurred after the paid request. All owned resources were removed; captured
host fields matched baseline. Comprehensive readiness and study dispatch remain unrun.
