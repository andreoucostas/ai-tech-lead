# B-41 implementation report — 2026-08-13

## Scope implemented

Implemented locked-design section 2 steps 1, 2, 2b, 3, 5, and 6, in order. Step 4 (the S1
Copilot schema spike and `~/.copilot/config.json` mutation) was not touched. No commit or push was
performed; all changes remain unstaged.

## Files changed

- Gate twins: `scripts/validate-dist.ps1`, `scripts/validate-dist.sh`.
- Maintainer records: `CHANGELOG.md`, `meta/BACKLOG.md`, this report.
- Core shipped sources: `src/core/docs/enforcement-surfaces.md`,
  `src/core/tests/evals/README.md`; deleted `src/core/tests/evals/run_evals.py` and
  `src/core/tests/evals/requirements.txt`.
- Dotnet stack sources: `src/stacks/dotnet/files/CHANGELOG.md`,
  `src/stacks/dotnet/files/docs/ARCHITECTURE.md`, `REVIEW-GUIDE.md`, `architecture.html`, and
  `playbook.md`; deleted `src/stacks/dotnet/snippets/tests/evals/README.md/case-cmd`.
- Angular stack sources: `src/stacks/angular/files/CHANGELOG.md`,
  `src/stacks/angular/files/docs/ARCHITECTURE.md`, `REVIEW-GUIDE.md`, `architecture.html`, and
  `playbook.md`; deleted `src/stacks/angular/snippets/tests/evals/README.md/case-cmd`.
- Monorepo stack sources: `src/stacks/monorepo/files/CHANGELOG.md`,
  `src/stacks/monorepo/files/docs/ARCHITECTURE.md`, `REVIEW-GUIDE.md`, `architecture.html`, and
  `playbook.md`; deleted `src/stacks/monorepo/snippets/tests/evals/README.md/case-cmd`.
- Composer-generated dist changes in each of `dist/dotnet`, `dist/angular`, and `dist/monorepo`:
  `CHANGELOG.md`, `docs/ARCHITECTURE.md`, `docs/REVIEW-GUIDE.md`, `docs/architecture.html`,
  `docs/enforcement-surfaces.md`, `docs/playbook.md`, and `tests/evals/README.md`; deleted each
  dist's `tests/evals/run_evals.py` and `tests/evals/requirements.txt`.

The three `src/stacks/*/files/tests/evals/cases.yaml` files were retained unchanged. All three
`case-id` snippets remain; all three `case-cmd` snippets are gone, and composed output confirms
`case-id` still resolves.

## Commands and observed results

### Baseline and red evidence

- `git status --short; git rev-parse --short HEAD` — clean status, `8cd82e6`.
- Executed the unfixed PowerShell regex against `python run_evals.py` — `MATCH_COUNT=0`.
- Executed the fixed regex against the same input — `MATCH_COUNT=1`, capture `run_evals.py`.
- First scratch red-gate attempt with `pwsh ... validate-dist.ps1` — did not start:
  `pwsh` is not installed/on `PATH`.
- Second direct-script attempt under the current PowerShell process — did not start because the
  process execution policy blocks scripts.
- Scratch copy of `dist/dotnet`, deleted only `tests/evals/run_evals.py`, then ran
  Windows PowerShell 5.1 with `-ExecutionPolicy Bypass` and `-Check no-dead-instruction` — exit 1;
  the gate named README lines 18, 21, and 24. Scratch tree was removed after the observation.
- Post-edit `-Check no-dead-instruction` for dotnet/angular/monorepo — exits 0; respectively
  34/30/35 documented script references and 35 links resolved.

### Architecture derivatives

- `src/core/scripts/build-architecture-html.ps1` via Windows PowerShell 5.1 for all three stacks —
  exit 0 each; stamped SHA-1 values `0ce1ad...`, `157a05...`, `aa6969...`.
- `bash src/core/scripts/build-architecture-html.sh` on the same three input/output pairs — exit 0
  each; output SHA-256 matched the PowerShell-generated bytes in every stack.
- `rg -i "executable spec|model-graded rubric" .../architecture.html` — no matches (rg exit 1).
- Independently computed CR-stripped Markdown SHA-1 versus HTML `src-sha1` — match `True` for all
  three stacks.

### Coverage audit and shipped-instruction sweep

- Enumerated every file in `src/core/.claude/hooks/` and cross-referenced the named leaf test files
  with `rg` for hook names, Copilot events, `permissionDecision`, `additionalContext`,
  `New-CopilotEvent`, and `Get-Decision` — confirmed coverage for audit-trail, guard, route-prompt,
  and session-start; no new test built.
- Swept shipped Markdown for Python/Node/npm wording. Found the three stale eval README Python
  commands in each dist before rebuild, no other bare Python/Node script instruction, and legitimate
  `npm ci` package-manager commands in Angular and monorepo CI integration docs. Recorded in RCA.

### Compose and validation

- Requested `pwsh -NoProfile -File scripts/build.ps1 <stack>` ×3 — all three failed to start because
  `pwsh` is unavailable.
- Windows PowerShell 5.1 `scripts/build.ps1 <stack>` ×3 — exit 0; composed 164 dotnet, 160 angular,
  and 174 monorepo files.
- `git status --porcelain dist/` — **not empty**: it lists the expected generated tracked changes
  from this implementation. This literal requested condition therefore failed.
- Repeated all three builds and compared a full before/after SHA-256 manifest —
  `SECOND_BUILD_BYTE_IDENTICAL=True`, proving deterministic/fresh output despite the expected Git
  diff against HEAD.
- Requested `pwsh ... scripts/validate-dist.ps1 <stack>` ×3 — all failed to start because `pwsh` is
  unavailable.
- Windows PowerShell 5.1 full `validate-dist.ps1 <stack>` ×3 — exit 0 for dotnet, angular, and
  monorepo. All eleven checks passed, including template checks, no-meta-leak,
  no-dead-instruction, and hook registration.
- `git diff --check` — exit 0, no whitespace errors.

### Meta and hook suites

- Full meta suite first attempt — exit 1: its job runner could not resolve bare `powershell` from
  this sandbox's `PATH`.
- Full meta suite with the Windows PowerShell directory prepended to `PATH` — timed out after
  376.8 seconds without output.
- Direct `DocTruth.Tests.ps1` in this sandbox — exit 2, 5 passed / 2 failed. This did not reproduce
  under independent re-verification on the real host (see the orchestrating session's addendum
  below): the sandbox result was an environment artifact, not a real defect.
- Combined dist hook suites with the same host-path repair — timed out after 360.4 seconds while
  running Angular, before Monorepo. Reached dotnet and Angular suites reported 30 failures each in
  `Guard.Tests.ps1`, caused by Windows PowerShell 5.1 native-error wrapping/line wrapping and JSON
  escaping differences (`\u0027` versus `'`) between twins. Coverage-relevant leaf suites reached
  were green: AuditTrail 13/13, RoutePrompt 12/12 with one invariant skip, SessionStartFrameworkRules
  9/9 with one invariant skip, SessionStartHazard 18/18 with one invariant skip, SessionStartWiki
  12/12 with one invariant skip. Bash Copilot JSON branches skipped because this host has no working
  jq/Python interpreter in bash. Monorepo full hook suite did not start before timeout.

## Could not verify / uncertainty

- Exact `pwsh` commands could not run because PowerShell 7 is absent. Windows PowerShell 5.1 was the
  available substitute and is explicitly reported above.
- The full meta suite did not complete; direct DocTruth failed two pre-existing-looking instrument
  assertions. No claim of a green meta suite is made.
- Full dist hook suites are not green on this host: dotnet and Angular exposed the same 30 Guard
  parity failures, and Monorepo was not reached before timeout. No claim of green hook suites is
  made.
- `git status --porcelain dist/` is not empty because the requested implementation necessarily
  changes committed generated output. A repeat-build byte manifest was identical, but that does not
  make the literal Git-status condition pass.
- No S1 schema re-verification was performed, by explicit user instruction to leave step 4 separate.

## Orchestrating-session addendum (2026-08-13, independent re-verification on the real host)

Sol's sandbox (Windows PowerShell 5.1 only, no `pwsh`, `bash` without a working `jq`/Python) produced
several results that did not reproduce on the maintainer's actual box (`pwsh` 7.6.4 present, per
`CLAUDE.md`'s stated command surface). Re-run independently, per Maintenance rule 3 ("verify in the
environment that matters"):

- `scripts/build.ps1` ×3 under `pwsh` — exit 0 each, 164/160/174 files composed.
- `scripts/validate-dist.ps1` ×3 under `pwsh` — **exit 0 each, all checks including
  `no-dead-instruction` passed.**
- Red-tested the regex fix directly: planted a `python tests/evals/ghost_runner.py` reference in a
  scratch dist and confirmed `no-dead-instruction` fails it (exit 1, names the file); the real
  (unmodified) dist passes clean.
- `dist/dotnet/tests/hooks/Invoke-HookTests.ps1`, `dist/angular/...`, `dist/monorepo/...` under
  `pwsh` — **0 failures across 16 files in all three dists**, including `Guard.Tests.ps1` at
  66 passed / 0 failed in dotnet. The sandbox's 30-failure Guard reports did not reproduce; they were
  a PowerShell-5.1-only sandbox artifact, not a defect in this diff.
- The meta suite's one real finding under `pwsh`: `DocTruth.Tests.ps1`'s phantom-marker check flagged
  this report file itself, because an earlier draft of this file quoted (verbatim, as running text)
  the exact marker string the check exists to catch — a self-inflicted false positive from this
  report's own prose, not from any shipped/tracked B-41 content. Fixed by rewording above. Confirmed
  by `git stash -u` against the pre-B-41 baseline (commit `8cd82e6`) that `DocTruth.Tests.ps1` was
  clean (7/0/0) before this change, then clean again (7/0/0) after this report was reworded.
- One separately pre-existing, unrelated environment defect was hit and worked around: this session's
  `$env:PATH` carries the known corrupted `${PATH}` placeholder (see `CLAUDE-HANDOFF.md`'s B-54 note
  and the `corrupted-session-path` record), which makes `FrameworkDoctor.Tests.ps1`'s 5.1 child-host
  probe falsely fail. Fixed per-process with the documented remedy and re-confirmed 31/0/0 clean;
  this is unrelated to B-41 and was already known.
- `git status --porcelain dist/` is correctly non-empty pre-commit (expected generated diff); this
  resolves once the change is committed.

