# B-144(a)/(b) investigation — 2026-08-18

## Step 1 — shipped `set -e` sweep

Scope: every `.sh` below `src/` and `dist/` containing `set -e`, `set -eu`, or
`set -euo pipefail`. Twelve files qualify:

- `src/core/scripts/build-architecture-html.sh`
- `src/core/scripts/install.sh`
- `src/core/scripts/sync-agent-files.sh`
- `dist/dotnet/scripts/build-architecture-html.sh`
- `dist/dotnet/scripts/install.sh`
- `dist/dotnet/scripts/sync-agent-files.sh`
- `dist/angular/scripts/build-architecture-html.sh`
- `dist/angular/scripts/install.sh`
- `dist/angular/scripts/sync-agent-files.sh`
- `dist/monorepo/scripts/build-architecture-html.sh`
- `dist/monorepo/scripts/install.sh`
- `dist/monorepo/scripts/sync-agent-files.sh`

Result: **no hits**. I found no command in those files that is both (1) a bare statement under the
task's definition and (2) legitimately non-zero in normal operation.

The high-value near-misses were reviewed rather than counted as hits:

- `build-architecture-html.sh:15` uses `[ -f "$src" ] || ...`; lines 18–19 use `command -v` in
  `if`/`elif`. These are conditions, not bare statements.
- `install.sh:61` uses `cmp` beneath `if !`; line 69 uses `grep -q` beneath `if ... && !`; lines
  119–120 use `[ ... ] &&`; line 124 has `|| true`; line 164 places tests/greps in `||`, `if`, or
  `&&`; line 171 explicitly guards the normal no-match `grep` with `|| true`; lines 174 and 197
  place tests and `command -v` in `if`. These are not bare statements. Ordinary `cp`, `mv`, `rm`,
  `mkdir`, `cat`, `awk`, `sed`, and output-redirection failures remain intentionally fatal rather
  than legitimate normal outcomes.
- `sync-agent-files.sh:13` guards `git rev-parse` with `|| pwd`. Its line 28 pipeline runs only after
  creating the destination; a failure of `find`, `wc`, or `tr` there is an actual operational
  failure, not a normal alternate result.

The three `src/core` scripts compose byte-equivalent copies into each dist, but every generated
copy was included above because the requested scope explicitly included both trees.

## Step 2 — minimal fixes for REAL hits

None proposed: the sweep found no REAL hit. Adding `|| true` anywhere on this evidence would hide
an actual error rather than state an intended normal alternative.

## Step 3 — meta-suite artifact/exit audit

“Artifact” here means the task's generalisable class: a produced file/directory, its content, or a
mutation to one. “Exit” means an explicit assertion of the producing external command's status.
Static/parser-only suites have no producing command and are marked N/A rather than being credited
with an exit assertion they do not need.

| Suite | Exit | Artifact | Judgement |
|---|---:|---:|---|
| `BacklogHygiene.Tests.ps1` | N/A | N/A | Static repository-text checks. |
| `DocClaims.Tests.ps1` | N/A | yes | Fixture mutations test in-process assertions; no external producer. |
| `DocTruth.Tests.ps1` | both | both | Mostly static; its child 5.1 run asserts `$LASTEXITCODE`; file existence/content is also checked. |
| `FidelityCheck.Tests.ps1` | yes | no | Captures and asserts child exit/output. |
| `GateBudgetConsistency.Tests.ps1` | N/A | yes | In-process budget object/file fixture; no external producer. |
| `InstallerContract.Tests.ps1` | **no** | no | Runs installers and asserts stdout only; it asserts neither installer exit nor delivered files. Not an artifact-only hit, but exposed to the same partial-success class. |
| `LicenseDelivery.Tests.ps1` | yes | yes | Every installer case asserts expected exit and legal-file state/content. |
| `LicenseDrift.Tests.ps1` | N/A | yes | Static file-content comparison; no producer. |
| `MetaHooks.Tests.ps1` | **no** | **yes** | **HIT:** invokes each BOM-fix hook and asserts file bytes/content, but never asserts the `Invoke-Hook` result's `Exit`. |
| `PushAndCheck.Tests.ps1` | yes | yes | Asserts subject exits and stub log presence/content. |
| `ReleaseChangelogStamp.Tests.ps1` | N/A | yes | Executes an extracted in-process scriptblock; exceptions fail the test, but there is no child exit code. |
| `ReleaseCiWatch.Tests.ps1` | yes | yes | Asserts watcher exits and recorded stub calls. |
| `ReleaseGateWaiver.Tests.ps1` | N/A | yes | Static AST/text and fixture assertions; no external producer. |
| `ReleaseStagingGuard.Tests.ps1` | yes | yes | Asserts child exits plus index/worktree state. |
| `RepositoryPrivacy.Tests.ps1` | yes | no | Captures and asserts command/script exit and output; repository files are inputs. |
| `RootInstallerWarehouse.Tests.ps1` | **no** | **yes** | **HIT:** runs the root installer and asserts the installed `adopt.md` exists, but does not assert installer exit. |
| `ScriptTwinCoverage.Tests.ps1` | N/A | yes | Static file/parity-source inspection; no producer. |
| `SkillListParity.Tests.ps1` | N/A | yes | Static file-content comparison; no producer. |
| `UpdateDelivery.Tests.ps1` | yes | yes | Already fixed: update/greenfield/refusal cases explicitly assert exit as well as delivered state. Some earlier per-property invocations still rely on the dedicated all-dist exit test. |
| `ValidateDist.Tests.ps1` | yes | yes | Captures validator exits; several cases also assert generated record streams/files. |
| `WorkspaceBom.Tests.ps1` | N/A | yes | Static corpus/fixture byte checks; no external producer command. |

Actionable artifact-without-exit hits: `MetaHooks.Tests.ps1` and
`RootInstallerWarehouse.Tests.ps1`. `InstallerContract.Tests.ps1` is adjacent exposure: its only
asserted product is stdout, so a late non-zero exit after printing the contract could pass.

No code was changed for Task 1.
