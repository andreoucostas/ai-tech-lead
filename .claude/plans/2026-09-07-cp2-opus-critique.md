# CP2 — requested Opus adversarial review

**Disposition: ACCEPT as a proposal for user approval**, following source corrections and a final
Opus check of the revised bytes. Opus initially returned REVISE, then narrowed its grounds after
root verified the source and requested corrections. The companion proposal incorporates the
retained findings; CP2 scope approval and execution evidence remain pending. This is a design review, not release
validation, an application result, or authorization to run the study.

**Frozen inputs and route.** Authoring baseline `a81a732a18ae9d04f4b1bc0ea3b0ee2a07c263e2`;
original proposal SHA-256 `7715CC4789A1E349B19657D8A32B2BB39C900BCDA035584FDBD604FA814A0F7C`.
Terra prepared an external pre/post source packet from the already known integration; root checked
all 29 entries in its manifest, whose SHA-256 is
`D8DC669F65719E440C23D9F374373E53592E08359A224880BA151F18D7710744`.
No new candidate search occurred. Source identities, paths, answers, prompts and raw transcripts
remain outside authoring Git and all actual setup/task scopes.

The independent initial Claude CLI 2.1.260 session requested `opus`, medium effort, read-only
Read/Glob/Grep tools, strict MCP configuration and no session persistence. Its initialization and
result metadata resolved the primary model to **`claude-opus-5`**. The prompt asked for a blind-first
threat model from the root rules and frozen CP1 contract before reading CP2, and withheld prior
review narratives. Subsequent fresh Opus sessions checked root's source counterevidence and the
final wording; these were corrective follow-ups, not additional independent blind reviews.

**Final delta evidence.** Revised proposal SHA-256:
`B73359455C81A78DC561961A8ECACB2F8B923B12D90A06AC0594833F9D60B694`, verified by root against both
the repository file and external frozen copy. The first delta attempt could not examine the file
because root's copy used the wrong working directory; its refusal to issue a verdict is retained.
The corrected delivery verified the absolute path/hash before invocation. Opus read those bytes,
returned ACCEPT, and found all six proposal-level conditions met. Root verified the text against
that result; the source baseline still follows the existing scope review and adds no new stage.
All four CLI invocations exited 0; the first delta's process success is not a successful review.
Their metadata report `claude-opus-5` as primary model, with ancillary Haiku calls and approximately
USD1.75 total list-price cost; that is CLI accounting, not an invoice or Copilot credit usage.

| Finding | Source-checked disposition and proposal change |
|---|---|
| Offline testing allegedly impossible | Opus retracted the claim. The source includes a pure calculation and injected I/O dependencies, so an offline harness is constructible in principle. Its fixtures, test host, dependency closure and actual execution are still unproved. Require the concrete harness to compile and run offline; do not grade a missing instrument as a solution defect. |
| Two decisions allegedly inseparable | Opus retracted the strong claim after checking distinct producer and consumer failure states. A shared symptom could still double-count a decision. Require each targeted violation red while the other decisions remain green. No three-decision qualification is claimed from source inspection alone. |
| Historical tests allegedly mostly warning-only | The source has five test methods: four assert output, one asserts only warning absence. Opus retracted its contrary count and unexecuted mutation claim. Retain the narrower risk of unwired warning capture: require a positive capture control plus values/kinds or behavior. |
| Alternative implementations may fail the checker | No CP2 oracle exists yet; the historical helper-name test is not its instrument. Require acceptance independent of undeclared helper names. Distinguish application build failure under a valid frozen contract from checker incompatibility with a supported alternative. |
| Retrieval probes overstate isolation | Finite negative probes cannot prove universal absence of opaque provider behavior. Name and verify observable controls/configuration and disclose residual uncertainty as potentially unequal between arms. Opus retracted its suggestion that a shared channel makes the confound cancel. Required controls stay fail-closed; no network relaxation is authorized. |
| Review consumes the remaining preparation budget | Charge another half-hour: 4.5 of eight preparation hours charged, 3.5 remain. Every stage is bounded by min(its original ceiling, remaining global budget). Opus retracted its proposed mandatory-stage deletion: ceilings are maxima, not required runtimes. |

**Root adjudication.** Reviewer corrections are evidence to check, not verdicts to inherit. Root
also corrected its own abbreviated interface names and incomplete constructor enumeration in the
external follow-up record. A failed guessed source path and its successful corrected read are
retained as access evidence, not a source defect. Source constructibility does not establish a
working test. Opus's suggested two-decision fallback is rejected: failure to establish three
decisions stops CP2. Its focused first application baseline remains possible only after the existing
short scope review, within the original stage, following user approval. No task, oracle, baseline,
network implementation, hidden-fact freeze, adoption or paid Copilot study call ran in this review.

**Proportionality and remaining gaps.** These are bounded clarifications to existing stages, not
new evaluation machinery. Credential-free dependency availability, offline application execution,
independent red/green controls and the actual network boundary remain unproved. The generic image
and source manifests are not readiness evidence. Original credit allocations and disabled overage
remain unchanged. Raw initial findings and explicit retractions are preserved; the earlier Sonnet
acceptance applies only to its frozen proposal, as recorded in the separate historical critique.
