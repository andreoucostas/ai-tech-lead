# RK1 resumption proposal for adversarial review

Status: REVIEWED / REVISE. The original proposal below is retained; the disposition at the
end supersedes its proposed execution sequence. No design is locked or execution authorized.
User requested Opus adversarial review
on 2026-09-09 after the next-work recommendation. This turn authorizes review and records,
not renewed container work, Copilot calibration or study dispatch.
Authoring baseline: `d14b662` (v0.86.2). The experiment remains pinned to the revision-1
v0.86.0 foundation `458a298ad4a876445c8e2ec5c0ea411435a39cf3`; the newer release does not
silently amend either arm. Governing contract: `2026-09-09-rk1-execution.md`, WSD-081 and
`meta/repository-knowledge-component-study.md`. Original deadlines remain expired.

## Decision and proportionality

The decision is whether to expand the released discovery increment, repair a demonstrated
delivery weakness, or hold further expansion. Four paired tasks can supply a bounded marginal
comparison; they cannot establish general productivity, independent adoption or VS Code behavior.
No material benefit has yet been measured. Repeated preparation stops are observed cost;
they are not framework failures or proof of impossible isolation.

Alternatives: (a) hold RK1 until an independent FS2 participant or naturally ready execution
route appears, with no more bespoke infrastructure; (b) finish the retained four-task comparison
through bounded reuse of the existing route. Prefer (b) only if retained tasks can be certified
and remaining route work is small. A single synthetic success or another transport nonce does
not answer the product decision. No new generic proxy, evaluation harness or release gate.

The previous recommendation put transport first. Amend that ordering prospectively: first
check the retained four candidates and their known test-discovery gap, then construct their
offline acceptance, then spend on connected transport. Some offline container feasibility
already exists; another network checkpoint is poor value if these candidates cannot qualify.
This is a proposal to amend revision 1's host-first ordering, not a rewrite of its history.

## Proposed staged attempt, requiring renewed execution authority

Use at most four additional hours including preparation, review, records and cleanup, measured
from an explicit future start. Stage ceilings sum to that bound; unused time is not transferred
to a failed stage. Passed stages proceed automatically within the authorized attempt. A failed
prerequisite or stage timeout stops and retains evidence; no automatic retry or sample change.
The paired study itself is not included in this preparation extension.

| Stage | Ceiling | Required result |
|---|---:|---|
| A: retained candidate audit | 30 minutes | Recheck frozen card/population hashes, four-area/knowledge-shape coverage and a concrete executable test route per task, including the excluded Angular specs. Source inspection is not a passing baseline. Stop if the retained set cannot meet the frozen selection contract. |
| B: offline task and variant readiness | 120 minutes | Observe each task's baseline, valid solution, supported alternatives and targeted decision violations with nonzero intended test counts. Independently review frozen oracles and the exact CONTROL source/installed-byte delta, with an unrelated-removal red control and restored clean comparison. If incomplete, stop before network work. |
| C: native tool transport and access | 60 minutes | Review the minimal external adapter amendment, run non-model controls first, then bounded synthetic native file/shell/observer calibration. Complete the live-service request-limit control and actual permitted/excluded access checks before declaring readiness. |
| D: readiness record and cleanup | 30 minutes | Reconcile usage, retain launcher/policy/receipt hashes, record every unrun control, clean exact owned resources and independently review the evidence. Produce READY only for the specifically observed configuration. |

Cleanup time is reserved within each resource-owning stage; stage D is final verification,
not permission to leave resources alive after an earlier stop. Root owns cleanup, with exact
owned IDs/labels, checked exits and before/after host fields. No global prune, recursive computed
cleanup outside verified owned roots, host upgrades, broad ACL changes, purchase or overage change.

## Preserve the experiment

Retain the original newest-first 100-integration prefix and four provisional cards; no candidate
101, replacement repository or retrospective reselection. Source/hash mismatch is cannot-examine
until explained, not permission to reacquire a new sample. If executable eligibility or coverage
fails, preserve the failure and stop this proposal. Do not silently replace a failed candidate.

Keep v0.86.0 ENHANCED and its experimental CONTROL ablation on the same foundation. Preserve
all unrelated repairs, manual wiki/navigation and project-pattern paths. Review source hunks,
installed artifacts and residual knowledge; no whole-file rollback across overlapping changes.
Existing installed validators must pass except explicitly declared treatment-presence assertions;
those intentional failures must be reported, never labelled green. Generated setup artifacts
remain as produced; a later setup-caused invalid state is an outcome, not a fixture to repair.

Task requests, solutions, graders, source history and other-arm/prior-session artifacts stay
outside setup and task scopes. Accept supported alternative implementations and ask/unresolved
where frozen evidence warrants it. Grade the requested behavior, not a forced reading method,
an undisclosed answer-format checklist or similarity to the historical patch. Keep hidden grading
keys while making all behavioral requirements part of the actual task contract.

## Transport calibration acceptance

Existing evidence is one native Copilot 1.0.83 / gpt-5.4 medium HTTP-fallback response through
relay-mediated auth, tools disabled and a 256-output-token clamp. It establishes neither ordinary
tool fidelity nor complete isolation. The late second-request probe was cannot-examine because
the relay expired. Do not relabel that historical observation or restart it to regain a paid slot.

A prospective calibration uses a new explicitly budgeted run identity. First exercise the
request-slot state machine without provider spend, including an observable available-slot state,
consumption before forwarding, refusal of excess requests while the service remains live, and
failure/timeout paths that cannot regain slots. Ordinary tool use needs multiple model turns:
freeze a finite multi-request allowance and cumulative usage bound for that separate calibration,
rather than applying the old single-request limit to the tool loop. Exact allowance and payload
policy require concrete code review before dispatch; a plan alone cannot certify them.

Keep the native client/tool implementation, fixed provider destination and credential boundary;
no BYOK, custom tool bridge, arbitrary CONNECT/redirect routing or disabled audit policy. Remove
the nonce-only payload clamp for study-fidelity testing only under the reviewed calibration cap;
record remaining payload/header changes and their comparability limits. If ordinary transport
requires a general proxy rewrite, stop and reassess proportionality.

Demonstrate own-file read/edit and shell success separately, plus denial of coordinator, other-arm,
history, oracle and prior-state access using matched positive controls. Check direct network and
alternate authority paths against reachable controls. Finite canaries support the declared threat
boundary, not universal containment. Observe native read/write/shell events and resulting bytes;
an echo-only response must not satisfy the access observer. Capture exact argv, identity, policy,
host/model route and partial output on failure. Test owned PS compatibility surfaces on direct
PS7 and PS5.1 without relaunching one through the other; do not infer service-host parity where
only PS7 executes the service. Calibrating an observer cannot require its paid observation first.

## Money, authority and subsequent study

The prior campaign record conservatively totals USD 7.0900995 of the USD 10 Claude ceiling,
including a full timeout reservation. This requested design review uses at most a USD 1.50 CLI
cap with headroom from that remaining envelope; reported usage can exceed a CLI cap and is not
an invoice. Reconcile actual review receipts before proposing implementation/reviewer allocations.
Do not assume the remainder funds all stage-B/C/D reviews. Insufficient budget means stop or
include a concrete prospective extension in the execution proposal; no hidden transfer/reset.

Retain the original 1,600 Copilot-credit envelope: 160 calibration, eight setups at 80, eight task
arms at 100. Keep the outstanding 30-credit calibration reservation until reconciled; fresh balance
and complete remaining allocation are prerequisites. This review spends no Copilot credits.
Preparation renewal must specify a feasible Claude allocation and exact calibration request/time
caps before launch. Until then this is a sequencing proposal, not a fully launchable runbook.

After READY, seek a concrete renewed study window with the complete four-pair allocation and
frozen alternating order. Keep original materiality thresholds, source/task/arm hashes, same exact
model/host and eight isolated setups plus eight task arms. Report nulls, regressions, void pairs,
setup cost and unobserved human effort honestly. No observed CLI result fills VS Code or FS2.

## Review questions

Challenge the premise before the mechanics: does the next product decision justify four more
hours; is waiting materially better; does the task-first amendment reduce expected waste?
Are stage B and its independent reviews plausible within their ceiling? Are the remaining cost
and ordinary multi-turn transport assumptions credible, or does the proposed attempt already
require disproportionate engineering? Identify any altered experiment, circular readiness test,
unreachable success state, false-green/cannot-examine confusion or authority ambiguity.
Return ACCEPT, REVISE or REJECT for this proposal with exact blockers, smallest corrections,
and explicit execution gaps. Acceptance cannot authorize live work or certify readiness.

## Opus review and root adjudication

Fresh Claude CLI 2.1.260 / `claude-opus-5`, xhigh effort, safe/restricted Read-only session
`632e748f-6261-443f-a083-18cc73ecd508` returned REVISE, result subtype success, process exit 0,
without timeout. Reported list-price cost USD 0.8299065 includes USD 0.001294 attributed by
the CLI to auxiliary Haiku usage; the principal review model is Opus 5. This is not an invoice.
Frozen proposal SHA-256: `087CB7136D80DC57EC1837579A17524D7D07ECEF93C2250B8AF91EDD7D27F846`.
Raw result SHA-256: `D3E90FD2B3BA82CFC39F42DB3978AA1109CC3B4921D5317FF99844F506CD92FF`.
External packet: `C:/TEMP/rk1-resumption-review-20260909` (proposal, supplied evidence, prompt,
launcher and raw result). The reviewer reports a blind-first threat model and source reads;
this non-streaming receipt retains only the final response, so root cannot independently
verify the ordered tool trace. Opus executed no commands or tests and recomputed no hashes.

Accepted corrections:

- CONTROL construction has no explicit work allocation; review cannot consume a nonexistent
  variant. Treat construction/composition and independent installed-byte review as a distinct
  downstream package, not hidden work inside task-oracle preparation.
- Separate a cheap candidate/coverage audit from actual restore/build/test execution. A named
  command or inspected config is not an executed route. The original wording blurred the boundary.
- Four task oracles, alternatives and reviews plus tool transport are not established as feasible
  within four hours. Stage ceilings limit spend; they are not completion estimates. Do not lock
  those bundled stages or claim READY from another isolated calibration.
- Native multi-turn requests require a reviewed finite request allowance, tool/body policy,
  retained credential/access boundary and a configuration capable of the actual setup/task path.
  All implementation/reviewer allocations and remaining credits must be concrete before launch.

Corrections to the review, independently checked against the retained files:

- The final native relay already parses caller-supplied JSON bodies and forwards the native
  payload after a declared clamp; it is not the earlier fixed-request relay. Root read the
  JSON, tool-rejection, rewrite and forwarding branches in the retained `relay.ps1`. Allowing
  tools and multiple requests needs review, but caller bodies do not newly destroy an established
  no-body property. Ordinary HTTP/SSE transport must not be misclassified as a new WebSocket
  or streaming platform requirement merely because the earlier fixed-HEAD contract prohibited it.
- A 30-credit CLI minimum is a cap/reservation, not a minimum charge or smallest spend unit.
  The prior record's measured roughly one-credit nonce makes that distinction explicit.
- Prior bounded authorization allowed automatic progression after passed stages. No rule requires
  another approval between every offline and live step when a future user authorization already
  covers both. This turn authorizes review only; no live work is authorized by this disposition.
- The proposed A-C-B-D order still sums to four hours at the stated ceilings, not Opus's suggested
  two and a half. Accept the cheap-screen-first idea, not that unsupported time estimate.
- WSD-081 has not been silently superseded: this remains an unlocked proposal. Record a standing
  ordering amendment at its authoritative decision only if a future design is accepted; a new
  standing decision/index change is unnecessary to record the present rejected sequencing.

Revised recommendation, not yet execution-authorized: only a 30-minute read-only audit of the
retained four cards/population, coverage and available test-route evidence is the next bounded
candidate. Its result is either a concrete known disqualifier, cannot-examine, or continued
provisional eligibility with an itemized execution/cost gap; none is READY or task acceptance.
No build, dependency restore, container/network mutation, new model call or CONTROL construction
belongs to that audit. Use its evidence to compare waiting against a separately budgeted route
completion and then CONTROL/oracle construction. Do not automatically enter either workstream.
This narrowing is root's disposition, not an Opus acceptance of a revised design.

The conservative RK1 Claude total is now USD 7.920006 of USD 10, leaving USD 2.079994 before
future reservations. The outstanding 30-credit Copilot calibration reservation is unchanged;
no Copilot call occurred here. No task card, campaign selection, shipped source or dist changed.
The original four-hour attempt is not approved; RK1 and its outcome question remain open.

Record verification: root ran BacklogHygiene under direct native PS7 and PS5.1 at code page
437, observed the broken-index control exit 1 on both, then 10/0 clean on both. DocTruth ran
16/0 on each host, including its applied/restored scratch mutation. All clean suite exits were
0. These checks verify authoring-record consistency, not the proposal's feasibility. No new
tests or shipped artifacts were authored; `git diff --check` passed before commit.
