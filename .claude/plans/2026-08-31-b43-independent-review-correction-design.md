# B-43 independent-review correction — locked design

**Locked:** 2026-08-31 after reading WSD-055, WSD-057, WSD-062, WSD-066, the original B-43
contract, and the independent blind-first review of
`9124fea8c53b6cd3c0e4138a9191c908b0f343db..e0f7ff80fe46932aec2825559957d3def4f50f05`.

## Premise and proportionality

The rejected candidate corrected the event table but left five exact assurance spellings elsewhere:
three READMEs promoted installer file arrival to automatic Copilot consumption; three review guides
let dated CLI canaries certify every registered event; and the always-loaded carrier plus AGENTS
mirrors treated enabled VS Code Preview hooks as current prompt/guard behavior. These are observed
contradictions of WSD-066, not a request for a generic claim classifier.

The smallest correction replaces only those claims, preserves native `.github/instructions/`
delivery as independent from hook lifecycle, and adds the five historical spellings to the existing
exact-claim denylist. No provider run, runtime branch, new suite, semantic prose inference, recurring
work, or blanket host downgrade is authorised.

## Frozen wording and authored scope

1. In all three stack README sources, replace “Copilot receives those rules automatically” with:
   “The update proves file arrival, not Copilot host consumption; see
   `docs/enforcement-surfaces.md` for dated, client-specific consumption evidence.”
2. In all three stack review-guide sources, state that dated canaries cover only exercised
   capabilities, not every registered event; Copilot CLI `agentStop` firing/queue and current VS Code
   Preview-hook lifecycles remain unverified. Preserve Preview/off-by-default/org-gated and
   shell-write limits.
3. In `src/core/.github/instructions/framework-rules.instructions.md` and the matching Agentic
   Workflow text in all three stack AGENTS sources:
   - say the native instruction carrier and hook lifecycle are independent, and delivery of one
     proves no event in the other;
   - distinguish registered, observed, and instructed behavior; registration proves neither firing
     nor output consumption.
4. In each stack-owned `security-pass` snippet and matching AGENTS paragraph, say only that for a
   prompt matching the hook's bounded security vocabulary, the registered prompt hook emits the
   reminder when invoked. Host firing and output consumption require separate capability-specific
   evidence; the security rule holds without the hook.
5. Add five narrow, exact historical-spelling entries to `VendorClaims` with one provenance object,
   positive historical text, negative replacement/canonical text, and a reason per entry. Raise the
   seeded rule floor from four to nine. Do not add a semantic rule family to `ClaimTruth`.
6. Update the root changelog and B-43 closure/RCA to name the independently found surfaces. The
   existing consumer changelog already states the outcome and needs no duplicate bullet.
7. Compose all distributions and regenerate `meta/context-footprint.json`. Review every monorepo
   sibling; never edit `dist/` directly.

## Evidence contract

- Each historical spelling must be recovered from the rejected candidate and caught by its exact
  pattern while the replacement and canonical qualified prose remain accepted.
- Release-specific hostile evidence must reinsert every retired spelling, compose an affected dist,
  observe the intended content-specific RED, then restore source and dist byte-identically and rerun
  clean.
- Both footprint twins and both validator twins must agree. Static context must not exceed WSD-055;
  the carrier rewrite is expected to shrink rather than consume headroom.
- Follow-up review covers the correction delta and the full tag-to-candidate range. Windows local
  execution is not Linux evidence; exact-commit CI supplies both supported platform legs.

## Explicit exclusions

No live Copilot or VS Code run; no change to hook JSON, hook scripts, installer behavior, host
certification rows, WSD-066's evidence standard, or B-49; no inferred statement that native
instruction delivery proves a Preview-hook event; no broad regex such as `Copilot.*automatically`.
