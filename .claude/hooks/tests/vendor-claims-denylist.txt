# Superseded vendor-claim denylist — the machine-checkable half of B-55.
#
# WHAT THIS IS. Claims about what Copilot and Claude Code actually *do* are restated across many
# shipped assurance surfaces (docs/enforcement-surfaces.md, hooks.json, stack READMEs and review
# guides, the always-loaded carrier and mirrors, hook headers, and the presentation HTML).
# Corrections have repeatedly needed several surfaces at once — on 2026-08-18, B-147's
# correction had to land in the matrix, the _comment and the narrative simultaneously. This file
# lists claims we have already learned are FALSE, so the next vendor change fails a gate instead of
# quietly making six files wrong.
#
# WHO READS IT. `.claude/hooks/tests/VendorClaims.Tests.ps1`, and nothing else. Unlike
# scripts/meta-denylist.txt, this file is NOT read by a bash twin: the gate is a meta test and meta
# scripts are PowerShell-only by decision (meta/workspace-decisions.md WSD-005). So .NET regex
# syntax is available in full here — lookaround included — and there is no POSIX-ERE common subset
# to keep to. Do not "helpfully" restrict these patterns for a bash leg that does not exist.
#
# WHAT IT IS NOT. Neither this file nor its gate ships. That is deliberate and is the ownership half
# of the fix: a consumer may accurately document an older Copilot version, quote a superseded claim
# in their own changelog, or describe a different hook arrangement on purpose. Failing a consumer's
# build over our vendor-fact bookkeeping would be the wrong trade — the same boundary B-131 drew for
# changelog grammar one file over.
#
# CANONICAL HOME. docs/enforcement-surfaces.md is where a vendor-capability claim belongs. That is
# prose convention, not enforced by anything — this file only catches claims already known dead.
#
# Format: one directive per line. Blank lines and lines starting with '#' are ignored.
#   DENY <regex>          .NET regex, matched against the whole file text. Must be followed by...
#   REASON <text>         ...its supersession reason, on the very next directive line. The reason is
#                         the deliverable: a bare "forbidden string" finding teaches nothing and gets
#                         suppressed. A DENY with no REASON is a fatal parse error.
#   ALLOW <path-substr>   dist-relative path substring exempted from ALL deny patterns.
#
# SCOPE. The gate scans the composed dist/{dotnet,angular,monorepo} trees, skipping CHANGELOG.md
# and any dated version section (`### 1.2.3 — date`) inside other files. Those legitimately narrate
# claims that were true when written; see the README `## Changelog` excerpt, which quotes the
# pre-1.0.65 Copilot stdout behavior correctly, in the past tense, in a shipped file.
#
# ADDING AN ENTRY. Prove each regex TWICE before committing it (B-59/B-64's inert-check class):
#   (i)  it matches the real superseded text AS IT WAS ACTUALLY WRITTEN — recover it with
#        `git grep <phrase> <sha>^ -- src dist`, do not paraphrase from memory;
#   (ii) it does NOT match the corrected prose that replaced it, nor any legitimate current
#        sentence. `no equivalent analyzer` (NUnit, in enforce-standards/SKILL.md) is the live
#        near-miss that killed the first draft of entry 1.
# An entry that matches nothing is inert; one that over-matches blocks correct writing. Both fail
# silently. Only list claims that are now FALSE — not ones that are merely narrower than the truth,
# or this list accumulates style opinions and stops being trusted.

# --- 1. Copilot's turn-end event ------------------------------------------------------------------
# Was: "Copilot has no equivalent event." (README.md hook table, `Stop` row, all three dists,
# through v0.34.x). Anchored on Copilot within one clause so it cannot reach the live, correct
# "NUnit ... has no equivalent analyzer" in enforce-standards/SKILL.md.
DENY (?i)Copilot[^.\r\n]{0,60}\bno equivalent event\b
REASON Copilot CLI 1.0.72 ships `agentStop`; corrected 2026-07-27 in v0.35.0. Copilot's turn-end equivalent exists — see docs/enforcement-surfaces.md.

# --- 2. Copilot and userPromptSubmitted stdout ----------------------------------------------------
# Was: "**Copilot does not consume hook stdout for this event**" (README.md hook table,
# `UserPromptSubmit` row, all three dists, through v0.34.x).
DENY (?i)Copilot[^.\r\n]{0,60}does not consume hook stdout
REASON Copilot CLI 1.0.65 consumes `userPromptSubmitted` stdout; live-verified 2026-07-04. It delivers only the LAST registered entry, which is a cardinality constraint, not a consumption one — validate-dist check 13 (B-148) enforces that separately.

# --- 3. What a Stop hook's `reason` reaches -------------------------------------------------------
# Was: "`reason` is shown only to the user, never fed to the model." (boy-scout-check.ps1/.sh
# headers, all three dists, through v0.34.x — it survived there for months). `\breason` cannot match
# inside `stopReason`: there is no word boundary between "stop" and "Reason", so the true statement
# about top-level `stopReason` stays sayable.
DENY (?i)\breason[^\w\r\n]{0,3}(?:is|was)\s+shown only to the user
REASON A Stop hook's `decision:"block"` reason IS shown to Claude as a system reminder; the confusion was with top-level `stopReason`, which is the field that is user-only.

# --- 4. Copilot Boy Scout end-to-end delivery ---------------------------------------------------
# Was: "Copilot delivers the queued nudge at the next prompt" (presentation event table through
# v0.79.1). Only the next-prompt channel was observed; the preceding agentStop fire/queue write was
# never run end to end.
DENY (?i)Copilot[^.\r\n]{0,80}\bdelivers the queued nudge at the next prompt\b
REASON Copilot CLI's `userPromptSubmitted` delivery leg is observed, but live `agentStop` firing and its queue write are unverified; registration or a later delivery channel does not prove the full chain.

# --- 5. Installer file arrival is not automatic Copilot consumption --------------------------------
# Was: "Copilot receives those rules automatically." (all three stack READMEs through v0.79.1).
DENY (?i)\bCopilot\s+receives\s+those\s+rules\s+automatically\b
REASON The installer proves that the native instruction file arrived; file arrival does not prove a particular Copilot client consumed it. Capability-specific dated evidence belongs in docs/enforcement-surfaces.md.

# --- 6. Dated CLI evidence does not certify every registered event ---------------------------------
# Was: "dated Copilot CLI canaries cover their registered events" (all three review guides through
# v0.79.1). The canaries did not exercise the registered `agentStop` event.
DENY (?i)\bdated\s+Copilot\s+CLI\s+canaries\s+cover\s+their\s+registered\s+events\b
REASON Dated CLI canaries cover only the capabilities they exercised; evidence for prompt and tool events does not certify live `agentStop` firing or its queue write.

# --- 7. Hook-disabled state does not prove an exclusive delivery path ------------------------------
# Was the always-loaded carrier's exact pre-v0.79.2 off-state assurance. Native instruction delivery
# and hook lifecycle are independent capabilities.
DENY (?i)\bWhere\s+hooks\s+are\s+off\s+\(Copilot\s+VS\s+Code\s+without\s+Preview\s+agent-hooks,\s+Copilot\s+CLI\s+<\s+v1\.0\.65\)\s+this\s+text\s+is\s+the\s+\*only\*\s+thing\s+that\s+reaches\s+the\s+model\b
REASON Native instruction delivery and Preview-hook lifecycle are independent; an enabled or disabled hook setting does not prove which instruction carriers the client consumed.

# --- 8. Enabled Preview registration is not observed prompt/guard reinforcement --------------------
# Was the always-loaded carrier's exact pre-v0.79.2 enabled-state assurance.
DENY (?i)\bOn\s+Claude\s+Code\s+—\s+and\s+on\s+Copilot\s+where\s+hooks\s+are\s+enabled\s+\(CLI\s+≥\s+v1\.0\.65,\s+VS\s+Code\s+Preview\s+agent-hooks\)\s+—\s+these\s+rails\s+are\s+reinforced\s+by\s+a\s+per-prompt\s+hook\s+and\s+a\s+write-time\s+guard\b
REASON Registration proves neither client firing nor output consumption. VS Code prompt delivery is unverified and its guard has only one historical observation with host and extension versions unrecorded.

# --- 9. Enabled Preview registration does not prove automatic security prompting ------------------
# Was the stack security-pass sentence in the always-loaded carrier and AGENTS mirrors through
# v0.79.1.
DENY (?i)\bOn\s+Claude\s+Code\s+—\s+and\s+on\s+Copilot\s+where\s+hooks\s+are\s+enabled\s+—\s+a\s+`UserPromptSubmit`\s+hook\s+flags\s+these\s+automatically\b
REASON For prompts matching its bounded security vocabulary, the registered prompt hook emits a reminder when invoked; host firing and output consumption require separate, capability-specific evidence, and VS Code prompt consumption remains unverified.

# --- 10. Emitted prompt/session JSON is not blanket VS Code consumption ---------------------------
DENY (?is)(?:mode\s+with\s+Preview\s+agent-hooks\)\s+consumes\s+stdout\s+only\s+as\s+JSON\s+additionalContext|VS\s+Code\s+agent\s+mode\s+\(Preview\s+agent-hooks\)\s+inject\s+userPromptSubmitted\s+additionalContext\s+into\s+the|Copilot\s+\(CLI,\s+and\s+VS\s+Code\s+agent\s+mode\s+with\s+Preview\s+agent-hooks\)\s+consumes\s+stdout\s+only\s+as\s+JSON|on\s+Copilot\s+it\s+lands\s*(?:#\s*)?only\s+via\s+the\s+JSON\s+additionalContext\s+shape\s+emitted\s+below\s+\(CLI,\s+and\s+VS\s+Code\s+agent\s+mode\s+with|Copilot\s+parses\s+stdout\s+only\s+as\s+JSON\s+additionalContext\s+\(CLI,\s+and\s+VS\s+Code\s+agent)
REASON The scripts emit documented JSON shapes for non-Claude input; registration and emission do not prove current VS Code firing, consumption, or model delivery.

# --- 11. A Copilot-shaped guard deny is not blanket current VS Code enforcement -------------------
DENY (?is)(?:Copilot\s+block\s+\(CLI\s+\+\s+VS\s+Code\s+agent\s+mode\)\s+=\s*(?:#\s*)?permissionDecision\s+JSON\s+deny\s+on\s+stdout|Block\s+=\s+JSON\s+\{"permissionDecision":"deny",\.\.\.\}\s+on\s+stdout\s+\(superset\s+incl\.\s+hookSpecificOutput\)|Copilot\s+\(CLI\s+and\s+VS\s+Code\s+agent\s+mode\)\s*(?:#\s*)?honor\s+a\s+JSON\s+`permissionDecision:\s+deny`\s+on\s+stdout|Copilot\s+\(CLI\s+\+\s+VS\s+Code\s+agent\s+mode\)\s*(?:#\s*)?honor\s+a\s+permissionDecision\s+JSON\s+deny\s+on\s+stdout|Task\s+0\s+confirms\s+VS\s+Code\s+honors\s+this(?:\s+and\s+tolerates\s+the\s+extra\s+top-level\s+key)?)
REASON The guard emits a top-level and wrapped deny shape. Copilot CLI pre-tool denial is dated evidence; VS Code has one narrow 2026-06-25 observation with host and extension versions unrecorded.

# --- 12. Boy Scout script output is not proven model delivery -------------------------------------
DENY (?i)(?:SCAN\s+queues\s+findings\s+and\s+emits\s+Copilot\s+context|additionalContext\s+\(above\)\s+reaches\s+the\s+model|candidate\(s\)\s+flagged\s+to\s+the\s+model)
REASON The scan can write its queue and emit a Copilot-shaped response; live agentStop firing and model consumption remain unverified.

# --- 13. Direct script fixtures are not host-firing evidence --------------------------------------
DENY (?i)\*\*Hooks\s+actually\s+fire:\*\*\s+\x60echo\s+
REASON Piping fixture JSON directly into a hook proves parser and output behavior, not client firing or consumption.

# --- 14. An absent denial cannot diagnose one host cause ------------------------------------------
DENY (?i)No\s+deny\s+means\s+Preview\s+agent\s+hooks\s+are\s+disabled\s+by\s+you\s+or\s+your\s+GitHub\s+organization\s+administrator
REASON An absent denial is inconclusive: trust, launch, interpreter, event, payload, consumption, and observation can fail independently.

# --- 15. An absent instruction marker cannot prove non-delivery or an exclusive carrier -----------
DENY (?i)If\s+the\s+marker\s+does\s+not\s+come\s+back,\s+the\s+file\s+is\s+not\s+reaching\s+the\s+model,\s+and\s+repo-wide\s+\x60copilot-instructions\.md\x60\s+is\s+the\s+only\s+carrier\s+you\s+can\s+currently\s+rely\s+on
REASON A returned marker is positive evidence for that run; absence cannot distinguish non-delivery from model noncompliance or inability to observe, and proves no exclusive fallback.

# --- 16. File-based routing is canonical, not Copilot's exclusive live route ----------------------
DENY (?i)(?:the\s+\*only\*\s+routing\s+surface\s+Copilot\s+has|no\s+hook\s+injects\s+routing\s+context\s+there|Claude-only\s+just-in-time\s+salience\s+copy\s+of\s+§1)
REASON AGENTS section 1 is the canonical file-based routing definition; separately registered prompt-hook delivery is capability-specific and has dated Copilot CLI evidence.

# --- 17. Architecture registration is not an unconditional event lifecycle -----------------------
DENY (?is)Registered\s+for\s+Claude\s+Code\s+\(\x60\.claude/settings\.json\x60\)\s+and\s+Copilot\s+\(\x60\.github/hooks/hooks\.json\x60\)\.\s+\x60\x60\x60mermaid\s+sequenceDiagram\s+participant\s+U\s+as\s+Developer\s+participant\s+A\s+as\s+Agent\s+participant\s+H\s+as\s+Hooks\s+U->>H:\s+SessionStart
REASON Registration proves configuration only; each architecture arrow must remain conditional on the exact host event firing and says nothing by itself about consumption.

# --- 18. A registered local CLI hook is not an all-event execution guarantee ---------------------
DENY (?i)\|\s*Copilot\s+CLI\s+hooks\s+\(\x60\.github/hooks/\x60\)\s*\|\s*✅\s*\|\s*✅\s+\(run\s+locally\)\s*\|
REASON The hook files are registered locally; firing and output consumption vary by event and require the capability-specific evidence in docs/enforcement-surfaces.md.
