**Mechanism** — when project evidence selects a .NET or Angular DI seam, follow the established composition root, lifetime, and token/interface shape; this framework selects none of those mechanisms.
