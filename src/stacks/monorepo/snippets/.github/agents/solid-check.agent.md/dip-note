- Where the corresponding profile is evidenced, a single-implementation interface (.NET) or `abstract class`/token DI seam (Angular) on an injected service is **required** by DIP — never report it as bloat.
