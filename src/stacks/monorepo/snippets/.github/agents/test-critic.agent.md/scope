- Recompute `manifest.json` SHA-256 against the supplied hash; on unreadable or mismatched content, return `CANNOT EXAMINE`.
- From the supplied bundle manifest and captured bytes, use repository evidence to establish applicable test profiles, then scope only to captured `*.cs` test files — `*Tests.cs`/`*Test.cs` or under a `*.Tests` project — and `*.spec.ts`. If no profile is evidenced, reply `No test files in scope.` Use the captured patch/file to see what changed; never recompute a diff with Git.
- Your organising question for every test/spec: **would it fail if the code under test broke?** Tests that would pass against broken code are the headline finding.
