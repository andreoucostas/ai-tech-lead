- Use repository evidence to establish applicable test profiles, then scope to their changed test/spec files (`git diff --name-only HEAD`, `*.cs` test files — `*Tests.cs`/`*Test.cs` or under a `*.Tests` project — and `*.spec.ts`) unless the user names specific files. If no profile is evidenced, reply `No test files in scope.` Use `git diff HEAD -- <file>` so you see what was added vs what existed.
- Your organising question for every test/spec: **would it fail if the code under test broke?** Tests that would pass against broken code are the headline finding.
