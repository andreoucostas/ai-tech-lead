- Recompute `manifest.json` SHA-256 against the supplied hash; on unreadable or mismatched content, return `CANNOT EXAMINE`.
- From the supplied bundle manifest and captured bytes, use repository evidence to establish applicable profiles, then scope only to captured `*.cs` / `*.csproj` (.NET) and `*.ts` / `*.html` (Angular). If no profile is evidenced, reply `No files in scope.` Use the captured patch/file to see what changed; never recompute a diff with Git.
