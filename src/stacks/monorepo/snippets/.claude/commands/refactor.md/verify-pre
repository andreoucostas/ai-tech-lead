Run the touched stack's gates — .NET: `dotnet build` and `dotnet test`; Angular: `ng build` and `ng test --watch=false --browsers=ChromeHeadless`. They must pass before changing anything. If tests don't exist for the code being refactored, write baseline tests FIRST (see Step 2).
