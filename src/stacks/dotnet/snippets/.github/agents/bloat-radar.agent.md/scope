- Use repository evidence to establish whether the .NET profile applies, then scope to its changed files (`git diff --name-only HEAD`, `*.cs` / `*.csproj`) unless the user names specific files. If the profile is not evidenced, reply `No files in scope.` Use `git diff HEAD -- <file>` so you see what was added vs what existed.
