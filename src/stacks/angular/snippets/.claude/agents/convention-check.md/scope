3. Recompute `manifest.json` SHA-256 against the supplied hash; on unreadable or mismatched content, reply `CANNOT EXAMINE` and stop.
4. From the supplied bundle manifest and captured bytes, use repository evidence to establish whether the Angular profile applies. Only when it does, scope to captured `*.ts`, `*.html`, `*.scss`. Skip `.spec.ts`, `.test.ts`, `.d.ts`. If the profile is not evidenced or no eligible captured files exist, reply `No files in scope.` Never recompute the scope with Git.
