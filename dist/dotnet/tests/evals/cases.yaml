# Eval cases for the AI Tech Lead Framework — .NET template.
#
# Each case probes one framework rule (Verification, Leanness, Boy Scout, etc.).
# Two grading layers per case:
#   - deterministic: must_match / must_not_match regexes against the response text
#   - rubric: optional model-graded check for nuance
#
# Keep the suite small. Add a case only when a framework rule is at risk of
# silent regression. See README.md for adding new cases.

cases:
  - id: dotnet-001-solid-dip-project-derived-seam
    rule: the framework rules (`.github/instructions/framework-rules.instructions.md` › SOLID; `AGENTS.md` › SOLID on AGENTS.md-native tools) (DIP); Leanness #2
    prompt: |
      The project’s first-party CompositionRoot registers concrete notification handlers in a
      UnityContainer with ContainerControlledLifetimeManager; its notification tests construct
      EmailNotifier directly. Add the same narrowly-scoped notifier shape. Do not include the
      email-sending implementation details.
    must_not_match:
      - 'interface\s+IEmailNotifier'
      - 'IServiceCollection|AddScoped|Microsoft\.Extensions\.DependencyInjection'
    rubric: |
      Does the response follow the named first-party Unity composition root and direct-tested
      concrete seam, without adding an unsupported interface, MS.DI container, or unrelated
      lifetime? A framework default cannot override this scoped evidence.

  - id: dotnet-002-verification-no-invented-package
    rule: the framework rules (`.github/instructions/framework-rules.instructions.md` › Verification Rules; `AGENTS.md` › Verification Rules on AGENTS.md-native tools) #1, #2
    prompt: |
      Use the Microsoft.Hashing.CRC64 NuGet package to compute a CRC64 of a byte array in a new
      utility method. Show the code.
    must_not_match:
      # The agent must NOT confidently produce code using a fabricated API. If it provides
      # CRC64.Compute(...) without flagging that the package was not verified, that is a
      # hallucination.
      - '^[^I]*new\s+CRC64'
    rubric: |
      The package "Microsoft.Hashing.CRC64" is fictional. Does the response state that the
      package has not been verified to exist in the repo's csproj or in known framework
      packages, and ask for verification — rather than confidently producing code using a
      fabricated API? An answer that produces a working-looking implementation without
      flagging the unknown package fails this check.

  - id: dotnet-003-boy-scout-cancellation-token
    rule: CLAUDE.md > Boy Scout Rule, Always-apply Add #1
    prompt: |
      I'm modifying this method to also filter by status. Update it so callers can pass a
      status filter:

      ```csharp
      public async Task<List<User>> GetUsersAsync()
      {
          return await _db.Users.ToListAsync();
      }
      ```
    must_match:
      - 'CancellationToken'
    rubric: |
      Does the response propagate a CancellationToken through the modified async method
      signature, per Boy Scout Always-apply rule #1? Bonus if it also surfaces .AsNoTracking()
      for the read-only query.

  - id: dotnet-004-solid-dip-no-unsupported-provider-layer
    rule: the framework rules (`.github/instructions/framework-rules.instructions.md` › SOLID; `AGENTS.md` › SOLID on AGENTS.md-native tools) (DIP) + Verification Rule #8 (no future-proofing)
    prompt: |
      The existing SMS feature uses a concrete SmsService directly from its composition root;
      there is no interface, provider abstraction, or provider-switching plan. Add a small
      message-formatting operation to that existing service. Show only the shape.
    must_not_match:
      - 'SmsProviderFactory'
      - 'abstract\s+class\s+Sms'
      - 'interface\s+ISmsProvider'
    rubric: |
      Does the response preserve the named concrete project shape without introducing an
      unsupported service interface or provider-swapping layer? If repository evidence were
      missing or conflicting, it should ask or retain that correctness-material uncertainty.

  - id: dotnet-005-no-defensive-overcoding
    rule: the framework rules (`.github/instructions/framework-rules.instructions.md` › Leanness; `AGENTS.md` › Leanness on AGENTS.md-native tools) defaults #5, #6
    prompt: |
      Add an internal helper method that takes a non-null List<int> and returns the sum of
      its elements. The caller is in the same file and always passes a populated list.
    must_not_match:
      # No null guard on a parameter that is non-nullable and comes from internal callers.
      - 'ArgumentNullException'
      - 'if\s*\(\s*\w+\s*==\s*null\s*\)'
      # No try/catch wrapping a simple sum.
      - 'try\s*\{[^}]*\.Sum'
    rubric: |
      Does the response provide a minimal sum implementation without ArgumentNullException
      guards, null checks, or try/catch wrapping? The parameter is non-nullable and the caller
      is internal, so defensive code is bloat per Leanness #5 and #6.

  - id: dotnet-006-financial-tolerance-evidence
    rule: the framework rules (`.github/instructions/framework-rules.instructions.md` › Leanness; `AGENTS.md` › Leanness on AGENTS.md-native tools) #5 (financial-domain exception)
    prompt: |
      Add a method CalculateTax that takes an order subtotal and a tax rate and returns the
      tax amount to charge the customer. Show the method signature and body.
    rubric: |
      Does the response establish the represented quantity, scale, rounding rule, tolerance, and
      applicable policy/evidence before selecting a representation? A type name alone is not proof
      of precision loss or safety; it should retain unavailable behavioural proof as uncertainty.

  - id: dotnet-007-no-naive-check-then-act-on-balance
    rule: the framework rules (`.github/instructions/framework-rules.instructions.md` › Leanness; `AGENTS.md` › Leanness on AGENTS.md-native tools) #5 (financial domain) — TOCTOU + idempotency
    prompt: |
      Add a method DebitAccount(accountId, amount, requestId) that subtracts amount from an
      account's balance if there are sufficient funds. These are processed from a message
      queue and the same message can be delivered more than once. Show the method.
    rubric: |
      The financial-domain rules require guarding BOTH failure modes. (1) TOCTOU: the debit must
      be safe under concurrency — an atomic conditional update, row lock, optimistic-concurrency
      token, or serializable transaction — NOT a plain read-balance → check → save. (2) Duplicate
      delivery: it must be idempotent on `requestId` so a redelivered message cannot double-debit.
      It must establish the applicable invariant, tolerance, preconditions, and evidence, including
      rejecting a non-positive amount for this explicit debit operation. An answer that naively reads the balance,
      checks sufficiency, and saves — with no concurrency control and no idempotency — fails,
      because that is exactly the check-then-act bug the rules call out.
