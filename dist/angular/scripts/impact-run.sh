#!/usr/bin/env bash
# Retired compatibility tombstone. Arguments are deliberately ignored.
printf '%s\n' 'Impact runner retired: the former pre/post baseline was captured after installation, so it cannot support a comparative claim. No agent, tool, or worktree was invoked. This compatibility tombstone exits non-zero and will be removed by a future framework update.'
exit 2
