Write-route calibration, before applying the approved refresh:

1. Create the previously absent ordinary file `calibration/write-route.md` containing only
   `ordinary write route available`.
2. Create the previously absent file `.claude/skills/calibration-write-route/SKILL.md` containing
   valid minimal frontmatter with name `calibration-write-route`, a harmless description, and a
   body containing only `skill-path write route available`.

These two files are control deltas. Then read `.claude/commands/rebootstrap.md` Phase 3 and
`.claude/skills/remember-for-team/SKILL.md`. Apply only the exact approved change described in
`control/approved-refresh.md`; its path and diff hash are the complete owner authority. Report the
semantic source recheck and unresolved evidence. No other existing or new project content is
authorized.
