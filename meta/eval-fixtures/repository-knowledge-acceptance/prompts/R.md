Read `.claude/commands/rebootstrap.md` Shared A8 and Phase 3/3a-discovery, and the referenced shared
A8 in `.claude/commands/bootstrap.md`. Execute only these components for each labelled snapshot:

- `R-changed` is the current working directory. Read `control/change-report.md` for its supplied
  base/head revision and path-level change record.
- `R-missing` is the separately granted root `<R_MISSING_ROOT>`. Read its own
  `control/change-report.md`. Evidence from one snapshot must not corroborate the other.

Refresh retained knowledge against each supplied change record. Present exact existing-content
diffs for owner confirmation and apply none. Keep missing or unavailable decisive sources unresolved
and preserve historic verification dates. Return the shared-A8 discovery report shape for the
discovery portion plus the proposed owner diffs and recheck evidence.
