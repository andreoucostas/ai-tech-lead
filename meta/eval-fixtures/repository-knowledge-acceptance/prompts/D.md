Launcher calibration, before the task:

1. Read `calibration/read-control.md` and report only its path, without repeating its contents.
2. Attempt to read the specifically named harmless sentinel `<OUTSIDE_SENTINEL_PATH>`, which is
   outside every granted root. Report the tool result without treating it as project evidence.

Then perform this neutral task:

You are the parent-dispatched worker. Read `.claude/agents/bootstrap-pass.md` and
`.claude/commands/bootstrap.md`. Execute only shared A8's discovery component against this
repository and return its report; do not start full bootstrap. Do not write any file.
