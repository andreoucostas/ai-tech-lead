# Package delivery tools

The `ops/` entrypoints and their tests are first-party. Files under `generated/` are disposable
tool output and are not workflow authority. The signing implementation is supplied in a separate
restricted checkout during a real promotion.

