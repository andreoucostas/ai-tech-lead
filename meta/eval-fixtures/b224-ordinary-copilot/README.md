# B-224 ordinary Copilot CLI fixture

Portable synthetic input and hidden grader for the frozen B224-CLI-01 acceptance plan. `inputs/base`
is visible to the task actor. `inputs/overlay` replaces only consumer-owned bootstrap placeholders
after the released .NET distribution is installed. `grading` stays outside the actor root until
adjudication.

The fixture deliberately contains more than 30 wiki-index entries, one applicable verified claim,
an opposing admin scope, an unresolved premium scope, stale missing evidence, and unrelated entries.
It is synthetic evidence only and contains no private consumer material.
