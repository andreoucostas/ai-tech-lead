# Review ledger

One row per release, written by `.claude/scripts/release.ps1` and committed with the release it
describes. It records **whether** an independent review happened and what the reviewer re-ran --
never whether the review was any good, which no gate here can judge. A `reviewer: none` row is a
legitimate outcome, deliberately not a silent one: it files a post-ship review item in
`meta/BACKLOG.md`. See root `CLAUDE.md` > Maintenance model.

| version | date | evidence |
|---------|------|----------|
| v0.44.0 | 2026-08-02 | reviewer: none -- post-ship review owed. **Discharged 2026-08-03** (B-86): adversarial pass by codex CLI `gpt-5.6-sol`, every finding re-run here; 8 re-runs incl. the `HarnessIntegrity` red-test under Windows PowerShell 5.1. Filed B-92 (P2), B-93 (P2), B-94 (P3). |
| v0.45.0 | 2026-08-05 | B-97: implemented by codex (gpt-5.6-sol lead, terra/luna delegated); reviewed independently by Claude Opus 5 in a separate session, which re-ran three gate red-tests itself -- marker-expansion inventory (planted a deleted hash-marker snippet in route-prompt: old check printed OK, new gate exit 1), section-path citation (planted CLAUDE.md > SOLID: exit 1) and carrier-import (removed the import line: exit 1) -- plus a false-positive guard (prose 'CLAUDE.md > Conventions wins on any conflict' must NOT fire: it did not). Found four defects absent from the implementer's report, incl. a .ps1 gate blind to 15 of 117 markers and a .sh twin that could never finish. B-102: NO independent review -- found, implemented and verified in one session by one model; post-ship review filed as B-103 per Maintenance model #2. Its red-test (jq hidden, real python present) was observed going from exit 0 'write-guard INACTIVE' to exit 2 'Blocked write', against both src and the composed dist. |
| v0.46.0 | 2026-08-05 | reviewer Claude Opus 5 (separate session from both implementers, higher tier); plan adversarially reviewed by codex gpt-5.6-sol (5 blocking findings, 4 accepted, 1 refuted by execution); re-ran the red-tests independently against byte-exact HEAD copies rather than trusting implementer reports: route-prompt Store-stub case EXIT=0/0 bytes pre-fix vs 1379 bytes post-fix, FrameworkDoctor.Tests vs pre-fix doctor EXIT=2 (18 passed, 2 failed, the python-only guard row red); also re-ran validate-dist x3 EXIT=0, hook suites x3 EXIT=0, meta suite EXIT=0 (ValidateDist 0 skipped, was 1 false skip); implementer Claude Sonnet (two rounds), whose round-1 verification had used a PATH where the defect cannot occur |
| v0.47.0 | 2026-08-06 | reviewer: none -- post-ship review owed |
| v0.48.0 | 2026-08-06 | reviewer: none -- post-ship review owed |
| v0.49.0 | 2026-08-07 | B-96 skill diff: adversarial review by codex gpt-5.6-sol, separate session, different model -- 4 blocking + 6 non-blocking findings; 8 accepted, 1 rejected (the /map-warehouse slash form is pre-existing convention at bootstrap.md:327), 1 deferred. Two were material: read-side rule 6 was written as an absolute and is WRONG for late-binding designs where the fact stores a durable key; and "one row per foreign-key column" permitted an EMPTY edge list -- confirmed by reading the ship-gate fixture, where fact.FactSales declares no FOREIGN KEY at all. validate-dist performance work: implemented by sol, independently re-verified by me. sol declared it verified under Windows PowerShell 5.1 and could not run bash at all; my pwsh + bash re-verification found a twin divergence sol could not have seen -- `-Check ''` exited 2 on PowerShell but ran ALL ELEVEN checks on bash ([ -n "$CHECK_ARG" ] cannot distinguish flag-absent from flag-present-but-empty). Fixed with a CHECK_SET sentinel; both twins now exit 2 identically. Red-tests I ran myself, each seen RED then GREEN: context-footprint ceiling planted +200 chars of monorepo frontmatter -> "FAIL: monorepo static.claude is 48081 chars, ceiling 48000", EXIT=1, then green. Eval self-test broken on two axes I chose independently of the implementer (edgeRows threshold 3->99; hasQueryRules regex neutered) -> EXIT=1 each, then 20 PASS. ReleaseGateWaiver.Tests broken by making the waiver fail open (the 2 cases written for that fired) and by renaming the extracted decision function (extraction failed loudly, 9 failed rather than silently verifying nothing) -> restored 11/11. NOT INDEPENDENTLY REVIEWED, stated plainly: the meta-runner parallelisation, -AllowFailingGate, and meta/gate-budget.json were implemented AND tested by me alone. A post-ship review is owed on those three. The 1,335s-vs-399s regression in my own first parallel runner was caught by measurement, not by review, which is the argument for that post-ship review rather than against it.  |
| v0.49.0 | 2026-08-07 | B-96 skill diff: adversarial review by codex gpt-5.6-sol, separate session, different model -- 4 blocking + 6 non-blocking findings; 8 accepted, 1 rejected (the /map-warehouse slash form is pre-existing convention at bootstrap.md:327), 1 deferred. Two were material: read-side rule 6 was written as an absolute and is WRONG for late-binding designs where the fact stores a durable key; and "one row per foreign-key column" permitted an EMPTY edge list -- confirmed by reading the ship-gate fixture, where fact.FactSales declares no FOREIGN KEY at all. validate-dist performance work: implemented by sol, independently re-verified by me. sol declared it verified under Windows PowerShell 5.1 and could not run bash at all; my pwsh + bash re-verification found a twin divergence sol could not have seen -- `-Check ''` exited 2 on PowerShell but ran ALL ELEVEN checks on bash ([ -n "$CHECK_ARG" ] cannot distinguish flag-absent from flag-present-but-empty). Fixed with a CHECK_SET sentinel; both twins now exit 2 identically. Red-tests I ran myself, each seen RED then GREEN: context-footprint ceiling planted +200 chars of monorepo frontmatter -> "FAIL: monorepo static.claude is 48081 chars, ceiling 48000", EXIT=1, then green. Eval self-test broken on two axes I chose independently of the implementer (edgeRows threshold 3->99; hasQueryRules regex neutered) -> EXIT=1 each, then 20 PASS. ReleaseGateWaiver.Tests broken by making the waiver fail open (the 2 cases written for that fired) and by renaming the extracted decision function (extraction failed loudly, 9 failed rather than silently verifying nothing) -> restored 11/11. NOT INDEPENDENTLY REVIEWED, stated plainly: the meta-runner parallelisation, -AllowFailingGate, and meta/gate-budget.json were implemented AND tested by me alone. A post-ship review is owed on those three. The 1,335s-vs-399s regression in my own first parallel runner was caught by measurement, not by review, which is the argument for that post-ship review rather than against it.  B-113 fix (same release): CI run 31168445026 observed all eight job names, discharging the "assumed, not observed" caveat. Fix is structural -- New-Jobs derives the leg list from watch-ci.ps1's own -ExpectedJobs default by AST rather than restating it. Red-tested BOTH directions by me: widening ExpectedJobs with a brand-new leg (the exact 68cf0aa scenario that caused this) leaves the suite 18/18 GREEN because the stubs follow automatically; removing the parameter fails loudly at 14 failures rather than silently passing. Full meta suite now 0 failures across 10 files, with NO waiver -- the -AllowFailingGate flag was deliberately dropped from this run, and would have been refused as stale if passed. |
| v0.50.0 | 2026-08-07 | Reviewer: codex gpt-5.6-sol, independent read-only sessions, TWO passes (plan, then pre-tag diff). Peer tier, not higher -- recorded as such. Plan pass: 6 blocking, 5 accepted (skill-mirror gate already existed so a proposed check was DELETED; the 0/6 Skill result was shown not to transfer from read to write tasks, which restaged the whole plan measurement-first; eval declared unreachable as specified; snapshot-vs-authority conflict; pure-SQL install under-designed). Diff pass: 6 blocking, 4 accepted + 1 partially rejected with evidence + 1 accepted as a release gap. Accepted and fixed: (a) two false dimensional absolutes that would have SHIPPED to consumers -- "coarser dimension loses detail" (it is the normal many-to-one case) and "same key, same role" (role-playing dimensions deliberately differ in role); (b) not every failed lookup is a late-arriving member -- the skill now requires classifying the miss; (c) grader scored Pass from fact-DDL column tokens alone, so a load stamping constant -1 scored bound -- resolution check added; (d) fact-DDL parser required a trailing semicolon and would have failed correct SSDT DDL ending at ")" or "GO". Partially rejected with evidence: "the fixture still hands over regionOnFact=False" -- refuted by the data, the mixed fixture added RegionKey 3/3 while reading that map; the fair half (guard checks only literal phrases) accepted. Independently re-run by me, not taken from any self-report: harness -SelfTest EXIT=0 (22 groups); grader red-tested on EIGHT axes, each firing its own named assertion and green on restore -- newDimTables blinded, regionOnFact blinded, boundCustomer blinded, reachedAddEntity blinded, enriched-map heading, non-Dim-prefixed dim table, all three resolution conjuncts blinded, terminator reverted to ");". One FALSE RED was caught and discarded during this (a BOM-stripping write failed the suite for an unrelated reason). validate-dist x3: only version-stamp drift, which this release stamps. Hook suites x3 EXIT=0. context-footprint: static.claude UNCHANGED, only two SKILL.md#body entries moved. Baseline verdicts re-verified BY HAND against retained load procedures under the stricter post-review criterion; they did not move. Implementer and reviewer-of-record were the same session responding to an external reviewer. Post-change measurement arm is pre-registered and NOT yet run at tag time. |
| v0.51.0 | 2026-08-08 | reviewer: none -- post-ship review owed. **Discharged 2026-08-08** (B-121): independent review by Claude Sonnet 5, separate session from the implementer (codex), against HEAD `1c26516`. Re-ran validate-dist ×3 (dotnet/angular/monorepo) fresh -- all 8 checks pass on each, including check 6 (no-meta-leak), 7 (no-dead-instruction), 8 (hook-registration). Rebuilt all three dists from src and confirmed `git status --porcelain dist/` empty (freshness). Red-tested the no-meta-leak gate per DEVELOPING.md recipe: planted `WSD-999` in `dist/dotnet/README.md`, confirmed EXIT=1 naming `README.md:329`, restored, confirmed EXIT=0 -- the instrument was seen to fail before being trusted to pass. Ran the dotnet dist hook suite (15 files, 0 failures) and the meta suite (11 files, 0 failures, including `ValidateDist.Tests.ps1` case 20's own built-in red-test). No defects found in the v0.51.0 diff itself. Two pre-existing open items unrelated to this release surfaced from `meta/BACKLOG.md`: B-119 (dimension-binding post-change arm still unmeasured, blocked on API spend cap) and B-98 (routing remains probabilistic by design decision, not a defect). |
| v0.51.1 | 2026-08-08 | reviewer Claude Sonnet 5 (separate session); re-ran Get-PsExe directly under real Windows PowerShell 5.1 and pwsh 7 (old: returns pwsh under a 5.1 parent -- false green; new: returns the actual host under both), and reproduced the newly-exposed build-architecture-html.ps1 git-root fallback bug under real 5.1 outside a git worktree (old: terminating NativeCommandError; new: EXIT=0 clean fallback); implementer codex gpt-5.6-sol |
| v0.51.2 | 2026-08-08 | reviewer Claude Sonnet 5 (separate session); re-ran FrameworkDoctor.Tests.ps1 directly and hit a failure the closure did not describe, traced it to this box's known corrupted session PATH lacking the WindowsPowerShell v1.0 directory rather than a defect in the change (Get-Command powershell resolved nothing under the bad PATH, Get-Command pwsh did), then re-ran with PATH corrected: 22 passed 0 failed 0 skipped, confirming the new resolver test runs for real rather than being skipped; implementer codex gpt-5.6-sol |
| v0.51.3 | 2026-08-08 | reviewer Claude Sonnet 5 (separate session); constructed my own red-tests beyond the suite: a %00-encoded link target correctly rejected, a ../../../../etc/passwd traversal correctly rejected as escapes-this-dist, and the real composed dist/dotnet passes clean reporting exactly 35 relative inline links matching the closure claim; full ValidateDist.Tests.ps1 34/0/1 (honest jq/python skip); validate-dist x3 and meta suite green; version-stamp consistency double-checked directly via template-checks.ps1; implementer codex gpt-5.6-sol, went through two independent review rounds before reaching me; separately raised meta-suite gate-budget ceiling 300s->650s (meta/gate-budget.json) after two consecutive real refusals at 341.6s and 424.2s, recorded per this repo's own ceiling-revision policy |
| v0.51.4 | 2026-08-08 | reviewer Claude Sonnet 5 (separate session); B-63: full FrameworkDoctor.Tests.ps1 31/31 pwsh and 30/30 (1 honest skip) real Windows PowerShell 5.1 matching closure claim, plus directly unit-tested both twins registration-classification functions against the exact edge cases an earlier review found (single-quoted targets, case-varied BASH.EXE, negative controls) all correct on both twins; B-89: reproduced the bug myself under real 5.1 from a non-git directory (old: terminating NativeCommandError; new: clean fallback exit 0), completed the missing dist rebuild and backlog closure codex left undone, re-ran the sweep and filed one more site as B-123b rather than expanding scope; validate-dist x3 and freshness green on the merged result for both; implementer codex gpt-5.6-sol |
| v0.51.4 | 2026-08-08 | reviewer Claude Sonnet 5 (separate session); B-63: full FrameworkDoctor.Tests.ps1 31/31 pwsh and 30/30 (1 honest skip) real Windows PowerShell 5.1 matching closure claim, plus directly unit-tested both twins registration-classification functions against the exact edge cases an earlier review found, all correct on both twins; B-89: reproduced the bug myself under real 5.1 from a non-git directory, completed the missing dist rebuild and backlog closure codex left undone, filed one more site as B-123b; first CI attempt (155042b) went RED on all three linux-hooks legs (exit 127, could not make controlled jq executable) -- diagnosed as New-ParserProbeBin inheriting a :PATH polluted by the calling test's own loop across repeated fixture-bin construction, invisible on Windows because Git-Bash resolves chmod independently of inherited PATH; fixed by anchoring the affected subprocess calls to an explicit safe PATH, could not reproduce locally (no Linux available), CI itself is the verification for this specific fix; implementer codex gpt-5.6-sol |
| v0.51.5 | 2026-08-09 | reviewer: Claude Opus (2 independent rounds across commits 10c89d0, 3c060f2, 8c58924); re-ran .claude/hooks/tests/ReleaseChangelogStamp.Tests.ps1 directly under both pwsh and Windows PowerShell 5.1, EXIT=0 both (4/4); also AST-extracted Set-ReleaseChangelogHeads/Test-ReleaseChangelogHeads and drove the fresh/retry/inconsistent/mixed-state matrix directly; implementer: Claude Sonnet |
| v0.52.0 | 2026-08-10 | reviewer gpt-5.6-sol high; re-ran pwsh -NoProfile -File .claude/evals/run-agent-evals.ps1 -SelfTest; EXIT=0; implementer Codex |
| v0.52.1 | 2026-08-13 | B-41: implementer sol (gpt-5.6-sol), reviewer Claude Sonnet 5 (different session/tier) -- independently re-ran build.ps1 x3, validate-dist.ps1 x3 (all checks incl. no-dead-instruction), meta suite (14/14 files, EXIT=0), all three dist hook suites (0 failures each) on the real pwsh 7.6.4 host after the codex sandbox's PS5.1-only environment produced several non-reproducing failures; red-tested the no-dead-instruction fix directly (planted python doc reference, EXIT=1) then confirmed green; independently computed and matched architecture.html src-sha1 markers x3. B-135: reviewed and delta-reviewed by codex sol per its own BACKLOG Done record prior to this session. |
| v0.53.0 | 2026-08-17 | reviewer Claude Opus 5 (this session); implementer codex gpt-5.6-sol (3 separate sessions); reviewer independently re-ran: full validate-dist x3 x both twins EXIT=0; replayed the literal B-57 defect (renumber add-tests item 1 to 0) EXIT=1 on both twins with identical text; SkillListParity vacuous-pass probe EXIT=1; DocTruth planted-heading red x3; dist hook suites x3 = 0 failures |
| v0.53.0 | 2026-08-17 | reviewer Claude Opus 5 (this session); implementer codex gpt-5.6-sol (3 sessions); reviewer re-ran independently: validate-dist x3 x both twins EXIT=0; replayed the B-57 defect EXIT=1 both twins; SkillListParity vacuous probe EXIT=1; DocTruth planted-heading red x3; dist hook suites x3 clean; CI-red follow-up diagnosed to the CRLF shared fixture and fixed |
| v0.53.0 | 2026-08-17 | reviewer Claude Opus 5 (this session); implementer codex gpt-5.6-sol (3 sessions); reviewer re-ran independently: validate-dist x3 x both twins EXIT=0; replayed the B-57 defect EXIT=1 both twins; SkillListParity vacuous probe EXIT=1; DocTruth planted-heading red x3; CI-red follow-ups diagnosed to the CRLF shared fixture and to a real section-extractor twin divergence, both fixed, the latter red/green-proven in-memory |
| v0.54.0 | 2026-08-17 | reviewer Claude Opus 5 (this session); implementer codex gpt-5.6-sol; reviewer re-ran independently: licence byte-identical across all three dists; both installer twins refuse a conflicting legal file with identical text, exit 3, leave it untouched and leave no half-install; drift gate RED on a one-byte change; update completes exit 0 + banner on 3 dists x 2 twins, RED with the guard removed; pre-existing set -e abort confirmed at the v0.53.0 tag in a clean worktree |
| v0.55.0 | 2026-08-17 | reviewer Claude Opus 5 (this session); implementer codex gpt-5.6-sol; reviewer verified in the diff rather than the report: all three precision traps honoured (circular DI qualified to the self-referencing provider; no "Angular warns" claim anywhere in dist, grep empty; signal inputs stated read-only); monorepo sibling present for docs/defaults.md; add-component byte-identical across angular/monorepo and both .claude/.github mirrors; full validate-dist both twins EXIT=0 on angular and monorepo |
| v0.56.0 | 2026-08-17 | reviewer Claude Opus 5 (this session); implementer codex gpt-5.6-sol; reviewer measured rather than assumed: update silently clobbers consumer edits to skills/hooks/scripts/settings.json (protected + consumer-added survive); the rejected first design proven false by installing v0.51.0 untouched and finding 31 differing files; verified preflight precedes first mutation on both twins, settings backup round-trips a consumer edit, greenfield creates no backup, v0.54.0 legal refusal still exit 3; UpdateDelivery 27/27 and the ordering assertion red-tested by removing the preflight |
| v0.57.0 | 2026-08-17 | reviewer Claude Opus 5 (this session); implementer codex gpt-5.6-sol; reviewer verified the mechanism not the report: nudge fires fresh, throttles next run, re-fires only after an old epoch is written into the state file CONTENTS (my first test aged mtime and was wrong), unwritable .state leaves exit 0 and silent, wording audited for overclaim, drill-kit WSD has no fabricated SHAs; SessionStartVersionAwareness 9/9 both twins. TWO refused attempts, both from whole-tree sweeps and both from sandbox-authored files no hook could see: BOM missing on the new .ps1 (B-100 class) and a concrete home path in the implementer report (B-122 class). Both fixed at source, both recurrences recorded on B-100, and I swept the whole tree for each rather than fixing only the named file |
| v0.58.0 | 2026-08-17 | reviewer Claude Opus 5 (separate session; TIER PARITY with implementer, not above -- maintenance rule 2 post-ship review owed); implementer codex gpt-5.6-sol, whose sandbox had neither bash nor Windows PowerShell 5.1 and honestly reported both legs not-observed. Reviewer independently re-ran: src/core/tests/hooks/HazardCheck.Tests.ps1 under pwsh 7 AND Windows PowerShell 5.1, EXIT=0, 27/27, 0 skipped (bash leg actually exercised); dist hook suites x3 EXIT=0 (18 files, 0 failures each). Red-tests, each with the mutation diff-verified as APPLIED before the result was trusted: hazard-check.sh path-existence always-true -> 4 FAIL; status case catch-all -> 3 FAIL; valid_cal return 0 -> 1 FAIL; hazard-check.ps1 bare-filename lookup always-true -> 1 FAIL. Found and fixed 2 blocking bash-only defects invisible to the implementer: unquoted candidate word-split let the shell pathname-expand a wildcard against the cwd; an all-empty table row was skipped by .sh but reported by .ps1. |
| v0.59.0 | 2026-08-18 | reviewer Claude Opus 5 (this session, separate from the implementer); re-ran the B-147 red-test by swapping HEAD's route-prompt twins into a scratch dist (mutation asserted present) -> RoutePrompt.Tests EXIT=4, 4 failures across both twins including the empty-routing+queue case; re-ran dist hook suites x3 EXIT=0 (0 failures across 18 files each) and RoutePrompt.Tests under Windows PowerShell 5.1 EXIT=0, the leg the implementer marked NOT OBSERVED; implementer codex gpt-5.6-sol |
| v0.60.0 | 2026-08-18 | reviewer Claude Opus 5 (this session, separate from the implementer); re-ran the B-59 red-test on a scratch dist with each mutation asserted applied -- broken SECRET pattern EXIT=2 blocked, broken TEST-DEFEAT pattern EXIT=0 allowed with a category-named warning; probed both guard twins on 7 case-sensitivity inputs, all 7 AGREE including Foo.CS now blocked by the bash twin that previously skipped it; re-ran dist hook suites x3 EXIT=0 (0 failures across 18 files each), meta suite EXIT=0 (22 files), Guard.Tests 82/0 under pwsh 7 and TwinParity 13/0 under BOTH pwsh 7 and Windows PowerShell 5.1; implementer codex gpt-5.6-sol |
| v0.61.0 | 2026-08-19 | Implementer: codex gpt-5.6-sol. Reviewer: Claude Opus 5, different session and tier. The reviewer did NOT rely on the implementer's self-report. Independently re-ran with the reviewer's own mutations: arm 4 red by swapping SOLID -> "Boy Scout Rule" in the heading list (exit 1); arm 7 red by disabling the vacuity guard, Count -ne 4 -> -lt 0 (exit 1). Both arms confirmed non-inert. Review found a defect none of the 8 implemented arms could see: Get-Content -Raw returns $null (not '') for an empty file, so the unguarded .Contains() threw and BOTH the "Protected-file sync" and "Framework rules delivery" rows vanished from the ps1 report (it printed "6 ok / 2 missing" with neither row present) while the .sh twin still reported "deferred" -- an inert diagnostic reading as a clean run, plus a twin divergence. Fixed; arm 9 added and observed red (exit 1) then green. Also caught: a meta-vocabulary leak the reviewer introduced in a shipped test comment, failed by no-meta-leak across all three dists, fixed. UpdateDelivery.Tests asserted the now-removed DIVERGED row; updated to assert deferral and extended with a half-migrated PENDING case -- 29/29 on both twins. Design rev 1 (ship the block manifest) was rejected by codex critique on four grounds, three of which the reviewer re-verified directly before accepting. |
| v0.62.0 | 2026-08-20 | Implementer: codex gpt-5.6-sol (B-152 data fix + gate rules 1-2). Reviewer: Claude Opus 5, separate session, higher tier. Reviewer independently: (a) planted a dated 0.99.0 head above the stamped 0.61.0 on a scratch dist, observed the pre-fix gate report "version stamps in sync" exit 0 -- a coverage loss the implementation introduced -- then added gate rule 3 and observed both twins red on that plant, bash exit=1 checked directly not through a pipeline; (b) ran the bash leg of validate-dist x3 and template-checks x3 (exit 0 each), the leg the implementer cannot execute; (c) ran the B-50 three-arm Copilot canary live on CLI 1.0.80 with positive and negative controls; (d) after a first refused release, diagnosed both meta-suite failures -- a 3000ms test timeout against a measured 1.8s idle runtime inside a 28-file parallel aggregate, and a BOM-less .ps1 under meta/ that my src/dist/.claude/scripts sweep did not cover -- fixed both and re-red-tested the first by removing the IsOutputRedirected clause. False-positive control (pre-stamp authoring state) green on all three dists, both twins. |
| v0.62.0 | 2026-08-20 | Implementer: codex gpt-5.6-sol (B-152 data fix + gate rules 1-2). Reviewer: Claude Opus 5, separate session, higher tier. Reviewer independently: (a) planted a dated 0.99.0 head above the stamped 0.61.0 on a scratch dist, observed the pre-fix gate report "version stamps in sync" exit 0 -- a coverage loss the implementation introduced -- then added gate rule 3 and observed both twins red on that plant, bash exit=1 checked directly not through a pipeline; (b) ran the bash leg of validate-dist x3 and template-checks x3, the leg the implementer cannot execute; (c) ran the B-50 three-arm Copilot canary live on CLI 1.0.80 with positive and negative controls; (d) diagnosed and fixed two meta-suite failures from a first refused release -- a 3000ms test timeout against a measured 1.8s idle runtime inside a 28-file parallel aggregate, and a BOM-less .ps1 under meta/ my directory-scoped sweep did not cover -- re-red-testing the first by removing the IsOutputRedirected clause; (e) after a second refusal on a red CI linux leg, established by rerunning the identical commit that the section-path failure is INTERMITTENT (same sha 358b2f8, previously red, rerun all 8 legs green), ruling out locale and content by local reproduction attempts. Filed as a flake rather than waived. |
| v0.63.0 | 2026-08-21 | Implemented by codex gpt-5.6-sol (B-100, B-83); reviewed and independently re-run by Claude Opus 5. DocTruth.Tests.ps1 observed RED without RELEASE_IN_FLIGHT_VERSION (9 passed / 2 failed) and GREEN with it set (11 passed / 0 failed) on both pwsh 7 and Windows PowerShell 5.1; BacklogHygiene.Tests.ps1 10 passed / 0 failed with the filed-against check blocking; DocClaims.Tests.ps1 2 passed / 0 failed. |
| v0.63.0 | 2026-08-21 | Implemented by codex gpt-5.6-sol (B-100, B-83); B-154 fix authored and reviewed by Claude Opus 5. Mutation red-test: reverting the newest-head skip made DocTruth.Tests.ps1 8 passed / 3 failed, restoring it 11 passed / 0 failed, on both pwsh 7 and Windows PowerShell 5.1. The first attempt (an environment-variable exemption) passed local gates then failed CI on both legs; that is recorded in the changelog and drove the hermetic redesign. ReleaseGateWaiver 11/11, ReleaseDistGateTiming 5/5, ReleaseChangelogStamp 4/4, DocClaims 2/2. |
| v0.64.0 | 2026-08-21 | Implemented by codex gpt-5.6-sol; reviewed and independently verified by Claude Opus 5. B-156 bash legs (declared UNRUN by the implementer rather than fabricated) executed by the reviewer with a stub grep exiting 2: framework-doctor.sh CANT-VERIFY naming a host/resource problem, exit 1; with the import genuinely removed, [MISSING] with a fix, exit 1; clean tree, 6 ok / 0 missing, exit 0; impact-run.sh host/resource message, exit 2, against a control at exit 0. B-138: byte-identical default-off proven with cmp against the pre-change runner on the same fixture (the evidence the implementer honestly could not produce), TIMING emitted when the variable is set, no collision with the RESULT regex, BOM efbbbf confirmed. ScriptTwinCoverage 1/1, WorkspaceBom 4/4, DocsSyncCheck 2/2, BacklogHygiene 10/10, shipped TwinParity 13/13, validate-dist green on all three dists. |
| v0.65.0 | 2026-08-21 | Implemented by codex gpt-5.6-sol; reviewed and independently verified by Claude Opus 5.  Consistency check red-tested by the reviewer: removing CLAUDE.md from install.ps1 $protected gave "ownership policy disagreement: consumer-owned/protected in install.sh but not install.ps1: CLAUDE.md" with exit 1 from build.ps1 AND build.sh, and exit 0 once restored -- so the check changes exit status rather than only printing, on both twins.  Twin-parity defect found and fixed in review [#3]: PowerShell Sort-Object is culture-aware and case-insensitive while sort(1) collates by locale, so the composers emitted byte-different manifests for an identical 163-path set. Both sides are now ordinal ([StringComparer]::Ordinal and LC_ALL=C), verified by composing with each twin and diffing: byte-identical. The implementer has no working bash and could not have seen this.  The implementer corrected the brief on three points, two substantive: $protected is not the whole preservation story (docs/wiki/INDEX.md is copy-if-absent, the LICENSE refuses to overwrite divergent consumer content) and install.sh expresses the policy as a case exclusion rather than a list.  validate-dist green on all three dists on BOTH the PowerShell and bash legs. |
| v0.66.0 | 2026-08-21 | Implemented by codex gpt-5.6-sol; reviewed and independently verified by Claude Opus 5.  The reviewer found the implementation INERT for two of four cases. Its empty-filter branch exited 1, but the mutation harness decides "the mutation was caught" from a non-zero exit and this suite's ordinary exit code IS its failure count, so "no cases matched" was indistinguishable from "one case failed". Demonstrated by removing the secret policy tag: 4 passed while two mutations exercised nothing. Fixed with sentinel exit 111, rejected by name in the harness. Re-verified both directions: tag removed -> 2 passed / 2 failed; restored -> 4 passed / 0 failed in 15s against a 234s baseline.  Shipped-suite regression check: with GUARD_TEST_POLICY unset, Guard.Tests reports 82 passed / 0 failed exactly as before, and the composed dist copies are byte-identical to source.  validate-dist green on all three dists. |
| v0.67.0 | 2026-08-21 | Implemented by codex gpt-5.6-sol; reviewed and independently verified by Claude Opus 5.  The contract decision the entry deferred was made by the reviewer before dispatch and is recorded in the changelog: none of the three extractor sites should swallow, because in every one the empty path is the PERMISSIVE path -- they report success for checks that never ran.  Bash legs verified by the reviewer (the implementer has no working bash and declined to claim them rather than fabricating output). Stub grep exiting 2: warehouse-map-check.sh, template-checks.sh and wiki-check.sh each exit 2 with a distinct host/resource message naming the file. Clean composed tree: all three exit 0. A genuinely non-warehouse repo still proceeds with no host claim -- that arm is what stops the fix trading a silent false pass for a noisy false failure, which is the mistake the first cut of the cheap half made in the opposite direction.  validate-dist green on all three dists on BOTH legs; TwinParity 13/13; ScriptTwinParity 9/9; BacklogHygiene 10/10 and DocTruth 11/11 after archiving B-156. |
