# Workspace decisions — framework-level ADR log

Lightweight, append-only. Cross-repo / meta-layer choices only; per-repo product decisions live in
each repo's `docs/architecture-decisions.md`.

## WSD-001: FRAMEWORK-CONTEXT.md baked into templates (Option B)
- **Date**: 2026-04-28
- **Status**: Accepted (implemented 2026-04-28)
- **Decision**: ship cross-repo context (shared libraries, multi-tenancy, dashboard contracts) as a
  `FRAMEWORK-CONTEXT.md` file inside each template, auto-populated by `/bootstrap` and re-scanned by
  `/docs-sync`, instead of a central `ai-framework-context` repo mounted via `--add-dir`.
- **Context**: agents in app repos need framework-level knowledge; a central repo adds an unverified
  cross-repo configuration step that silent-fails at onboarding and breaks offline/CI use.
- **Alternatives**: Option A (central context repo + additional working dir) — rejected for the
  onboarding silent-failure and offline/CI gaps.
- **Consequences**: self-contained repos; accepted drift cost, mitigated by `/docs-sync` + CI checks.
  (Folded from the retired `project_framework_architecture.md`, 2026-07-01.)

## WSD-002: AGENTS.md is a generated full mirror; Bitbucket Data Center is first-class
- **Date**: 2026-06-04
- **Status**: Accepted (implemented in v0.8.0+)
- **Decision**: `AGENTS.md` carries the full portable ruleset (generated by `/generate-copilot`,
  CLAUDE.md canonical); skills mirror to `.github/skills/`; the GitHub-only cloud layer is replaced
  on Bitbucket DC by `scripts/docs-sync-check.*` in Bamboo/Jenkins + Code Insights.
- **Context**: AGENTS.md-native tools (Copilot, Codex, Cursor, Gemini, Aider) don't read CLAUDE.md;
  a pointer file gave them a pointer instead of rules.
- **Consequences**: mirror drift becomes a real failure mode → machine-checked since WSD-006/v0.24.0.
  (Folded from the retired `project_framework_architecture.md`, 2026-07-01.)

## WSD-003: Remediate the 2026-07-01 forensic-audit findings in the dual-repo world now
- **Date**: 2026-07-01
- **Status**: Accepted
- **Decision**: fix all audit findings (v0.23.3 + v0.24.0) against the current two repos rather than
  waiting for the monorepo merge or folding fixes into it.
- **Context**: MERGE-MIGRATION-PLAN.md has no sign-off or date; drift had already shipped while
  ungated. Gates are written to retarget cleanly at merge time.
- **Alternatives**: minimal fixes now + gates in the merge; merge first — both rejected: they leave
  the framework ungated for an unbounded interval.
- **Consequences**: some gate wiring (cross-repo lockstep check, dual CHANGELOG bumps) will be
  re-targeted or collapsed by the merge; that cost is accepted.

## WSD-004: Workspace root is a git repo tracking the meta layer
- **Date**: 2026-07-01
- **Status**: Accepted (maintainer-approved during audit-remediation planning)
- **Decision**: `git init` the workspace root; track `CLAUDE.md`, `AGENTS.md`, `DEVELOPING.md`,
  `docs/`, `LEARNINGS.md`, `MERGE-MIGRATION-PLAN.md`, `.claude/` (hooks/scripts/plans/settings.json);
  ignore the two template repos, `presentation/`, `settings.local.json`, the session lock.
- **Context**: the meta layer had an invariant violation nobody caught (`bom-fix` without a `.sh`
  twin); untracked governance can't be diffed, reverted, or audited.
- **Consequences**: commit-and-push discipline extends to meta changes (local-only; no remote yet).

## WSD-005: Meta scripts are PowerShell-only; twin parity scoped to shipped artifacts + meta hooks
- **Date**: 2026-07-01
- **Status**: Accepted
- **Decision**: invariant #3 (.ps1/.sh twin parity) binds every **shipped** hook/script and every
  root meta **hook** (`bom-fix.ps1`/`.sh`, twin-tested); root meta **scripts**
  (`.claude/scripts/check-lockstep.ps1`, `release.ps1`) are PowerShell-only.
- **Context**: shipped twins exist for consumer cross-platform reach; the meta layer runs only on
  the maintainer's Windows box — a bash twin of the release automation is dead weight (Leanness #8).
- **Consequences**: if meta work ever moves to a non-Windows box, write the twins then.

## WSD-006: guard.sh warns loudly instead of blocking when no JSON parser exists
- **Date**: 2026-07-01
- **Status**: Accepted (shipped in v0.23.3)
- **Decision**: with neither `jq` nor `python3` on PATH, `guard.sh` allows the write but prints a
  `write-guard INACTIVE` warning to stderr; docs state the parser dependency honestly.
- **Context**: the header claimed secrets "fail closed", but the no-parser path allowed everything
  silently — a silent security-floor outage. Blocking outright would brick every write on minimal
  boxes.
- **Alternatives**: fail closed (block all writes) — rejected as unusable; stay silent — rejected as
  dishonest.
- **Consequences**: the floor's absence is observable in hook logs; installing `jq` restores it.

## WSD-007: template-ci.yml does not ship to consumers
- **Date**: 2026-07-01
- **Status**: Accepted (shipped in v0.24.0)
- **Decision**: the installer removes `.github/workflows/template-ci.yml` from consumer installs;
  consumers get the same deterministic checks via `docs-sync-check` → `template-checks`, wired into
  their own CI (`docs-sync-check.yml` sample ships).
- **Context**: template CI is the template repo's own gate (hook suite on every push); consumers on
  Bitbucket DC can't run it and GitHub consumers would run redundant hook tests on every app push.

## WSD-008: Self-sufficiency strategy is toolchain-first; wait-for-Copilot applies to inner-loop workarounds only
- **Date**: 2026-07-02
- **Status**: Accepted (roadmap recorded; nothing implemented yet)
- **Decision**: enforcement self-sufficiency for the real consumer profile (Copilot VS Code /
  Windows / Bitbucket DC) is pursued toolchain-first — analyzers/lint-as-errors via an
  `enforce-standards` skill, a verified Jenkins/Bamboo required-build recipe — plus a cheap port of
  `route-prompt`/`session-start` injection to Copilot surfaces, which now consume
  `userPromptSubmitted` → `additionalContext` (CLI GA v1.0.65+; VS Code behind Preview hooks).
  Rejected as P0: silent `core.hooksPath` git hooks, shipped `standards-scan` twins, pre-receive
  template, Code Insights publisher, regex leanness heuristics.
- **Context**: 2026-07-02 forensic review + adversarial critique
  (`.claude/plans/2026-07-02-self-sufficiency-forensic-review.md`, roadmap WS-1…WS-7). The review
  also found shipped stale claims ("Copilot discards userPromptSubmitted stdout" — no longer true)
  in `hooks.json` comments, `enforcement-surfaces.md`, and CLAUDE.md §1; fixing those is part of
  WS-2 regardless of the spike outcome.
- **Consequences**: waiting on Copilot is legitimate for inner-loop salience (Preview flag likely
  GAs) but never covers the toolchain/CI layer, which constrains humans and every tool.

## WSD-009: post-write `timeoutSec` unified to 120 across both repos; hooks.json is now gated
- **Date**: 2026-07-04
- **Status**: Accepted (shipped in v0.25.2; backlog B-05)
- **Decision**: set the Copilot `postToolUse` `post-write` `timeoutSec` to **120** in both repos'
  `.github/hooks/hooks.json` (was dotnet 120 / angular 60), and gate `hooks.json` in
  `check-lockstep.ps1` via a structured registration compare (`event|bash-script|timeoutSec` set
  equality, with `audit-trail` the one declared dotnet-only reg until B-14 ports it to Angular).
- **Context**: `hooks.json` is security-relevant (it wires guard/route-prompt/post-write/session-start
  on Copilot) yet was gated by nothing, and the timeout had already silently drifted. A higher ceiling
  is strictly safer: the hook self-throttles on frequency, so the timeout only bounds a single build/
  type-check — too low a value kills a cold `dotnet build`/`npx tsc` mid-run and drops its feedback.
- **Alternatives**: document the drift as deliberately stack-flavoured (dotnet slower than tsc) —
  rejected: the ceiling has no downside when generous, and one value is simpler to gate and reason about.
- **Consequences**: a self-test (`.claude/hooks/tests/CheckLockstep.Tests.ps1`) plants a timeout drift
  and asserts the gate catches it. When B-14 ports `audit-trail` to Angular, drop the `audit-trail`
  exception here and move `hooks.json` toward the IDENTICAL class.

## WSD-010: team wiki memory (B-27) — design fixed; implement from the design doc, don't re-derive
- **Date**: 2026-07-04
- **Status**: Accepted (design only — implementation pending, target v0.26.0)
- **Decision**: ship a PR-curated team knowledge layer per
  `.claude/plans/2026-07-04-b27-wiki-memory-design.md` (D1–D10): flat `docs/wiki/` with a
  **sorted, grammar-pinned INDEX.md** + one-fact-per-file entries reusing the hazard map's
  epistemic ladder (`verified|suspected|unverified` + `last-verified`); retrieval = CLAUDE.md
  pointer + capped session-start preload inside `$body`; write path = `remember-for-team` skill
  (triage tree, dedup with verify-before-bump); validation = `wiki-check.ps1/.sh` twins **called
  from docs-sync-check** (exit 0/1). Injection gate is split: markers in `description:`/INDEX
  lines **FAIL** (the index is auto-injected into every session), entry bodies warn only.
  Install follows an ownership matrix (INDEX copy-if-absent in all modes + `$adoptionSignals`
  entry; `_template.md`/`wiki-check.*` always copied); `/adopt` gains a **screen-in-place**
  class (screened, never archived/merged; flagged files quarantine-moved, deterministically
  reddening wiki-check). check-lockstep gets `docs/wiki/INDEX.md` + `_template.md` in
  `$identicalFiles`.
- **Context**: consumer dev pools have no shared agent memory beyond CLAUDE.md + append-only
  LEARNINGS.md; leading practice (Anthropic CLAUDE.md-as-code, Karpathy-style LLM wiki) says
  curated repo wikis compound while uncurated auto-capture rots. Locked constraints at triage
  (BACKLOG.md B-27): no RAG/embeddings/decay infra, no auto-append hook, PR review is the
  curation mechanism.
- **Alternatives**: migrate/absorb LEARNINGS.md — **rejected (headline reversal)**: ~21
  touchpoints incl. the machine-consumed `## Declined recipe:` contract; roles genuinely differ
  (history vs curated claims). Guard cred-scan exclusion for wiki paths — rejected (gotcha notes
  are where real secrets would leak). Append-order INDEX — rejected (EOF-append guarantees
  conflicts). exit=count — rejected (bash mod-256 wrap).
- **Consequences**: design survived two critique rounds (self + independent adversarial agent;
  15/15 findings incorporated, incl. one BLOCKER — the injection-gate split — and three
  "cited mechanism doesn't do that" corrections on adopt/check-lockstep/ci-integration). The
  implementing session must not relitigate D1–D10; deltas discovered during implementation get
  appended here, not silently applied.
- **Retarget note (2026-07-06, per WSD-012)**: target moves **v0.26.0 → v0.27.0**, implemented
  **once in the merged `ai-tech-lead` repo** after the monorepo migration (B-25-EXEC). D1–D10
  stand unchanged; only the *delivery mechanics* retarget — dual-repo lockstep [#1] and the
  check-lockstep `$identicalFiles` additions become composer/fidelity-gate additions, and the
  install ownership matrix maps onto the dist modes. The full mechanics appendix is written at
  merge Phase 5 and appended here.

## WSD-011: Token policy is the v0.8.0 static per-agent model tiering; no routing/budget framework
- **Date**: 2026-07-05
- **Status**: Accepted (records existing practice; no behavioral change)
- **Decision**: the framework's and meta layer's token-cost management is **static per-agent model
  tiering as shipped in v0.8.0** — no dynamic router, no token-budget framework, no additional
  multi-agent orchestration. The tiering rubric is the v0.8.0 one (CHANGELOG "Token economy →
  Model routing", both repos): **recurring, pattern-based work → `model: haiku`**
  (`convention-check`, `bloat-radar`, `debt-radar`); **high-stakes or one-time-high-leverage →
  `model: inherit`** (`security-auditor`, `solid-check`, `test-critic`, `bootstrap-pass`). This
  entry records and forward-references that rubric for future agents; it does not revise it.
- **Context**: prompted by the maintainer's question whether the framework should adopt a
  token-usage-management framework (cheap-model routing, agent "teams"). Primary-source consensus
  supports the existing shape and rejects heavier machinery: Anthropic's *Building Effective
  Agents* (prefer the simplest architecture that works) and multi-agent research write-up
  (multi-agent ≈15× chat tokens; pays off for parallelizable research, not context-coupled
  coding; the "token spend explains ~80% of performance variance" figure is from browsing-style
  research evals, not coding); Cognition's *Don't Build Multi-Agents* (coding is context-coupled
  — keep the main loop single-threaded on a strong model); FrugalGPT/RouteLLM (dynamic routing
  pays only on high-volume homogeneous request streams — neither surface here qualifies). Token
  economy in this framework is already handled as targeted point fixes (v0.8.0 routing +
  quiet-on-success hooks + CLAUDE.md size budget + ADR hot-path split; watch list B-26).
  **Epistemic status**: haiku-tier adequacy has been *assumed* since v0.8.0 — the CHANGELOG's
  "without losing security or bootstrap quality" claim was never evaluated (evals have never
  gated a release, B-23). B-29 files the evidence gap. **Copilot surface (implementation fact,
  not a capability claim)**: tiering is currently wired on the Claude Code side only; the shipped
  `.github/agents/*.agent.md` wrappers deliberately pin no `model:`. GitHub documents a `model`
  frontmatter property for IDE custom-agent profiles (CLI honoring tracked upstream) — unverified
  here, so tiering must never become load-bearing for correctness on either surface.
- **Alternatives**: dynamic router / token-budget framework — rejected (over-engineering; review
  rigor is the product, and a cheap-model quality regression costs more than the tokens saved;
  no request volume on the meta layer). Shipped consumer-facing cost-knobs doc (how `/review`
  agents are tiered, how to change it, per-surface cost implications) — **deferred, not
  rejected**: it is a shipped doc change (lockstep + CHANGELOG + version bump); revisit on
  consumer cost feedback. One-line B-26 watch-list addition instead of an ADR — rejected: the
  question was asked directly and deserves a citable record. Downgrading more agents to haiku —
  rejected (fails the v0.8.0 rubric's stakes test).
- **Consequences**: future agents get tiered by the v0.8.0 rubric, cited from here. B-29 (haiku
  adequacy evals) and B-30 (`test-critic` missing from both repos' ARCHITECTURE.md §5 agents
  table — shipped doc drift found during this investigation) are filed. Meta layer note: it
  already routes where it matters (`tests/evals/run_evals.py` pins the grader to Haiku;
  adversarial critique passes run as subagents per WSD-010) — no further mechanism.

## WSD-012: Monorepo merge SIGNED OFF (D1–D7) — merge first as v0.26.0, B-27 follows as v0.27.0
- **Date**: 2026-07-06
- **Status**: Accepted (decision + plan refresh done; execution pending as backlog **B-25-EXEC**)
- **Decision**: merge `ai-tech-lead-dotnet` + `ai-tech-lead-angular` into one **`ai-tech-lead`**
  repo per `MERGE-MIGRATION-PLAN.md` (refreshed same day). Signed values: **D1** name
  `ai-tech-lead` · **D2** first merged version **v0.26.0, merge before B-27** (wiki memory
  becomes **v0.27.0**, implemented once in the monorepo — avoids building its L-effort work
  twice) · **D3** audit-trail unification is already shipped reality (v0.25.3 + the v0.25.5
  B-31 hotfix), so the migration carries **zero intentional behaviour change**; residual
  stack-flavoured lines are a named fidelity allowlist · **D4** single root CLAUDE.md with
  `### .NET`/`### Angular` subsections, nested-files fallback if composed size >~1.5×
  single-stack (measured) · **D5** preserve both histories via `git filter-repo` ·
  **D6** `dist/` committed + CI rebuild-diff gate · **D7** the workspace meta layer moves into
  the merged repo at Phase 5 (governance files tracked; `check-lockstep` + its tests retire;
  `release.ps1` retargets; BACKLOG + this decision log move to the repo's `docs/`).
- **Context**: evidence re-measured at v0.25.4/5 — 138 tracked files per repo, 128 common
  paths (93%), **51 EOL-normalized-identical** (31 at the v0.18.0 draft), 10+10 stack-only;
  convergence *increased* under the v0.23–v0.25 gate work. Decider: a mixed .NET+Angular
  consumer repo cannot adopt at all today, and a third stack costs a whole repo instead of
  ~20 snippet/whole-files. The plan was adversarially reviewed (fresh-context agent; every
  number reproduced); its high findings are folded into the refreshed plan: the **B-31 shipped
  bug** it surfaced (angular `settings.windows.json` missing the audit-trail registration —
  fixed + gated in v0.25.5), stale counts/baselines in the execution sections, archive/tag
  moved **after** Phase 6 validation, and a binding **abort rule** (fidelity gate not
  allowlist-clean → stop, unfreeze, legacy repos stay live).
- **Alternatives**: stay two-repo — genuinely defensible post-v0.25.x (the gates tamed
  lockstep; drift now gets caught, not shipped); rejected because only the merge yields
  monorepo-consumer support and linear third-stack economics. B-27-first sequencing —
  rejected: pays dual-repo lockstep cost on L-effort work, then re-ports it.
- **Consequences**: both legacy repos freeze at Phase 0 (freeze scope: shipped-repo backlog
  items pause; meta-only design work like B-21/B-22 P0 docs continues). Invariant #1 collapses
  to the composer check at merge; #7 becomes one CHANGELOG/one stamp. WSD-010 retargeted (see
  its appended note). Successor model executes Phases 0–6 from the refreshed plan; deltas
  discovered during execution get appended here, not silently applied.
- **Phase 0 execution delta (2026-07-08, B-25-EXEC started):**
  - **Freeze ON.** Both legacy repos frozen at v0.25.5; shipped-repo items (B-15…B-23, B-29)
    paused, meta-only work continues.
  - **Fidelity baseline tags recorded + pushed:** `freeze-v0.25.5` →
    dotnet **`bd8bb2fb6550f3b3a7e80e837b814d7b9e4c9381`**, angular
    **`e0f77825a4dbc5b02f99d5e71a51cc16ea6d2c05`**. These SHAs are the Phase 3 fidelity baseline.
  - **D5 Windows hazard RETIRED — filter-repo path confirmed, no fresh-start bail-out needed.**
    `git filter-repo` 2.47.0 installed via `python` 3.14.5; verified on a `--no-local` throwaway
    clone of dotnet: `--to-subdirectory-filter legacy/dotnet` completed cleanly, all 138 files
    relocated, `git log --follow legacy/dotnet/CLAUDE.md` preserved all 38 commits to the initial
    commit. **Op note for Phase 1:** the Python Scripts dir
    (`C:\Users\Costas\AppData\Roaming\Python\Python314\Scripts`) is **not on PATH** — prepend it so
    `git filter-repo` resolves as a subcommand, or invoke the `.exe` directly. `python3` is the
    Windows Store shim; use `python`.
  - **Empty `ai-tech-lead` repo (D1): CREATED** by maintainer 2026-07-08 —
    `github.com/andreoucostas/ai-tech-lead`, **private**, empty (flip public at Phase 6).
    **Phase 0 COMPLETE.**
- **Phase 1 execution delta (2026-07-08, DONE):** history-preserving merge complete.
  filter-repo'd both repos into `legacy/{dotnet,angular}` (138 files each), then in the local clone
  of `ai-tech-lead`: seeded `master` from dotnet, `git merge --allow-unrelated-histories
  angular/master` — **zero conflicts** (disjoint trees). Result: 276 files, 119 commits, merge
  commit `305d69e` with both v0.25.5 parents. `git log --follow` traces both `CLAUDE.md` files to
  their v4.0 initial commits (`5fc9221` / `852357c`). Tagged **`pre-restructure`**; pushed `master`
  + tag to origin. **Branch decision: `master`** (GitHub created the repo defaulting to `main`;
  standardized to `master` to match the legacy repos + `release.ps1`'s `git push origin master` —
  GitHub default branch is now `master`). Local merged repo at `C:\TEMP\AIdrivenDev\ai-tech-lead`
  (git-ignored by the meta repo).
- **Phase 2 execution delta (2026-07-08 → 2026-07-09, COMPLETE — see the completion bullet below):** classification reproduced WSD-012 exactly
  (128 common, **51** EOL-identical, **77** differing, 10+10 stack-only). Git index already stores
  all blobs LF → no EOL work needed; set an authoring-repo root `.gitattributes` (deterministic LF).
  **Mechanical moves committed** (`ca819cd` in `ai-tech-lead`): 51 identical → `src/core/` (moved
  dotnet copy, deleted angular dupe → git renames, `--follow` verified back to v0.24.0), 20 stack-only
  → `src/stacks/<stack>/files/`. Repo now: src/core 51, src/stacks 10+10, legacy/ 77+77 remaining.
  **REMAINING = twin extraction of the 77 differing files** (core skeleton + `@stack:` snippets, or
  whole-file overrides above the ~6-marker cap) — the Sonnet-delegable bulk under Opus fidelity
  review. Recommendation for next session: stand up a minimal composer early so each extraction is
  verified against the freeze content as it lands, rather than authoring all 77 blind before Phase 3.
  - **Twin-extraction progress (2026-07-09): 135/138 reproduced, both stacks, mismatch=0.** Built
    the verify-as-you-go harness: `scripts/build.sh` (composer: core copy + `@stack:` marker
    substitution — markdown `<!-- @stack:NAME -->`, scripts/YAML `# @stack:NAME` — + stack-files
    overlay) and `scripts/fidelity-check.sh` (dist vs the `pre-restructure` frozen baseline,
    EOL-normalized). `dist/` is git-ignored during Phase 2 (Phase 3 commits golden dist).
    Extraction method: programmatic (`awk` line→marker, `sed` line→snippet) for byte-fidelity.
    Snippet convention: `src/stacks/<stack>/snippets/<core-relpath>/<NAME>` (absent snippet →
    marker removed for that stack; used for dotnet-only/angular-only lines like `.gitignore`
    prepend and `security-auditor.agent` line-13). Whole-file overrides (`git mv` both copies →
    `src/stacks/<stack>/files/`) for the >6-marker-cap set (bootstrap/adopt/…, metrics, defaults,
    cases.yaml, tasks.json, heavy agents/hooks/docs, enforce-* & add-tests skills, README,
    CHANGELOG, JSON). The fidelity gate caught two real errors mid-flight (angular `route-prompt.ps1`
    regex off-by-one; a CLAUDE.md line-drift) — both fixed by it. **PHASE 2 COMPLETE (`218acac`):
    138/138 files reproduced for BOTH dotnet and angular dists, mismatch=0/missing=0/extra=0 — the
    zero-behaviour-change proof (acceptance #1) for both single-stack dists.** `CLAUDE.md` = core
    skeleton + 28 `@stack:` snippets (the one authored single-source; count-differing
    Boy-Scout/OnPush blocks, angular +4-line drift). `AGENTS.md` + `copilot-instructions.md` =
    per-stack whole-file overrides (generated mirrors verified by the template-checks mirror gate,
    not authored sources). Empty `legacy/` removed. src/ = core 89, stacks/<s>/files 48,
    stacks/<s>/snippets 87. Commits `ca819cd`→`218acac`, pushed. **Next: Phase 3** — add the
    `build.ps1` composer twin, un-ignore + commit golden `dist/`, wire single-repo CI (rebuild +
    `git diff --exit-code` + hook suites + template-checks per dist), rewire the installer. Then
    Phase 4 monorepo mode (same-path whole-file-override collisions handled there).
- **Phase 3 execution delta (2026-07-09, COMPLETE — commit `6acb8e5`, pushed):** composer twin +
  golden dist + CI + installer, all verified. Deltas vs the plan text, in scope order:
  (a) **`dist/monorepo` deferred to Phase 4** — Phase 3's composer twins cover `dotnet|angular`
  only, matching the Phase-2 composer's declared scope; the monorepo mode is Phase 4 content work.
  (b) **Installer rewire = thin root wrappers** (`install.sh/.ps1` at repo root): stack resolution
  (flag → update-stamp `template` → auto-detect csproj/sln vs angular.json, root+2 levels; both →
  exit 2 pointing at Phase 4; neither → exit 2 asking for the flag) then **delegation to the
  byte-frozen legacy installer inside `dist/<stack>/scripts/`** — dist stays fidelity-clean, zero
  duplicated install logic, update mode confirmed accepting legacy stamps (`template` values match
  dist modes exactly). (c) **Fidelity gate now STRICT** — missing-in-dist fails too (the Phase-2
  "not-yet-extracted" tolerance would have let a composer regression silently drop files in CI);
  red-tested (missing/mismatch/extra → exit 1) on both twins. (d) The **fidelity allowlist is
  EMPTY** — 138/138 EOL-normalized byte-match for both stacks with no version-stamp or
  stack-flavoured exclusions needed (stronger than acceptance #1 demanded). Verification evidence:
  `build.ps1` output byte-identical to `build.sh` across the full matrix (PS 5.1 + pwsh 7 ×
  dotnet + angular, 276/276 files); `validate-dist` twins green ×4 and red-tested with 4 planted
  defect classes each; per-dist hook suites 2×(7 files, 0 failures); installer smoke matrix 9
  scenarios; post-commit CI-freshness simulation (rebuild + `git diff --cached --exit-code` on
  dist) green. Twin-authoring traps recorded in root `LEARNINGS.md` (pwsh 7.3 `-split` negative
  limit; PS 5.1 EAP=Stop × redirected native stderr; MSYS-vs-System32 `tar`/`bash` resolution).
  **Next: Phase 4** (monorepo mode: compose + token measure + hook union + Phase-4 items).

## WSD-013: B-21 reviewer-profile fixes — design LOCKED; implement post-merge (≥ v0.28.0)
- **Date**: 2026-07-06
- **Status**: Accepted (design only — implementation pending, post-merge, backlog B-21)
- **Decision**: lock the three reviewer-profile systemic fixes per
  `.claude/plans/2026-07-06-b21-reviewer-profile-design.md` (the normative spec — implement from
  it, don't re-derive). **D1**: `/bootstrap` Phase 4 and `/adopt` Phase 8 emit a prioritized
  "needs a human decision" checklist (INFERRED conventions, unsure hazards, adopt-defaulted
  contradictions, discovered skills) as plain yes/no engineering questions into a paste-into-PR/
  commit block — bootstrap gains a commit/PR landing nudge, adopt is the single emitter (bootstrap
  suppresses under adopt), and adopt-4a writes a `<!-- DEFAULTED: … -->` durable marker so its
  defaulted choices survive the embedded `/bootstrap` run. **D2**: `session-start.ps1/.sh` gain a
  hazard-staleness resurface — real interval math (cutoff = today−90d, ISO-pinned, inside
  `$body`/`emit_body` for Copilot delivery), which also pins 3d-bis's `Reviewed` date to ISO.
  **D3**: keep the `[VERIFIED]/[SUSPECTED]/[UNVERIFIED]` machine-anchor tokens but add a
  **rendered** (not HTML-comment) legend + a "merging the PR does not confirm these" line above
  the hazard table.
- **Context**: [[reviewer-profile]] — consumers' PRs are reviewed by competent engineers with
  very limited AI understanding; the pipeline must make every AI-architecture call so reviewers
  only ever answer plain questions about their own code. The design found the original backlog
  entry partially stale (adopt-4a contradiction prompts + bootstrap 3d-bis plain questions
  already shipped) and re-scoped to the true residual gaps: judgment items scatter and expire
  silently. Adversarially critiqued (fresh-context agent; **LOCK WITH AMENDMENTS**): two HIGH
  findings (D2's date mechanism was misdescribed and depended on an unpinned date; D1's block had
  no landing site + the defaulted-contradiction source had no durable trace) plus M/L fixes are
  all folded into the design doc, which carries the findings log.
- **Alternatives**: committed `docs/review-checklist.md` (rejected — new-file cost, rots at
  review end); replace ladder tokens with prose (rejected — breaks the D2 parser + 3d-bis
  writer + honour-instruction); `docs-sync-check` NOTE for staleness (kept as optional follow-on,
  reaches CI not the file-opener).
- **Consequences**: implementation is **post-merge, ≥ v0.28.0**, as single `src/core` edits
  (0.26.0 = merge, 0.27.0 = B-27); **independent of B-27** (the critique corrected an assumed
  dependency — B-27 put its staleness reporting in `wiki-check`, not session-start). Effort M–L.
  Frozen under WSD-012's shipped-work freeze until the merge lands. Deltas found at implementation
  time get appended here.

## WSD-014: B-22 headless `/adopt` — design LOCKED as "Path A" (prepare autonomously, human merges)
- **Date**: 2026-07-06
- **Status**: Accepted (design only — implementation pending, post-merge, backlog B-22)
- **Decision**: lock headless `/adopt` per `.claude/plans/2026-07-06-b22-headless-adopt-design.md`
  rev-2 (the normative spec). An installing agent can **prepare** adoption autonomously — branch,
  archive, provenance + adversarial screen, impact baseline — and **stage** every proposed
  `CLAUDE.md`/`TECH_DEBT.md` merge as a reviewable change on an `adopt-ai-framework` branch, but a
  **human applies the merges at PR review**. Nothing derived from untrusted external artifacts is
  *applied* to canonical guidance without a person (**Path A**). Invocation reuses the existing
  read-and-execute-the-workflow prompt pattern (`.github/prompts/adopt.prompt.md`) with a
  `--headless` directive — works on **both** Claude Code and Copilot CLI; `disable-model-invocation`
  stays on the slash command. The embedded Phase-7 `/bootstrap` runs headlessly too (3d-bis
  defaults every hazard `[UNVERIFIED]` + lists it). Composes with **B-21 D1** (the PR checklist).
- **Context**: [[follow-on-headless-adopt]] — the v0.20.0 handoff still leaves a human for the last
  step; the maintainer wants agent autonomy for rollout. Four locked constraints (BACKLOG B-22):
  safe-default + deferred artifact per gate; **quarantine stays human-gated (non-negotiable)**;
  solve the invocation path; review moves to the PR. The **fork decision** (2026-07-06): the
  adversarial critique (verdict RETHINK) proved constraint 2 (the prompt-injection boundary,
  financial domain) forbids auto-merging untrusted content into `CLAUDE.md` — a keyword denylist
  was the only automated filter and the interactive "show each merge" backstop would be gone. The
  maintainer chose **Path A** over Path B (constrained auto-merge with residual injection risk),
  reframing "headless" from *finish* adoption to *prepare* it. The critique's two HIGH findings
  (auto-merge boundary breach; the embedded `/bootstrap` 3d-bis stall) and M3–M7/L8–L9 are all
  folded into rev-2; the doc carries the findings log.
- **Alternatives**: Path B constrained auto-merge — rejected (residual injection reaches CLAUDE.md
  for an AI-naive reviewer to catch; unacceptable in a financial-domain framework). "Reconsider
  scope / don't build" — not chosen; Path A shrinks the human's job (review+apply) below today's
  full manual `/adopt`, which is worth building. Flipping `disable-model-invocation` — rejected
  (ambient model-invocability is the exact risk the flag exists to stop).
- **Consequences**: implementation **post-merge, ≥ v0.28.0**, single `src/core` edits; **depends on
  B-21 D1** (sequence with or before). Pulls a *scoped* slice of "bootstrap headless" (the
  adopt-embedded 3d-bis path) in scope. Frozen under WSD-012 until the merge. Deltas found at
  implementation time get appended here. Related open follow-on: the Copilot-CLI headless leg is a
  first-class target here (not deferred), but a live cross-surface verification is prudent at
  implementation (cf. the B-03 canary discipline).

## WSD-015: Monorepo dist composition — concat-by-default + authored override + collision error
- **Date**: 2026-07-10
- **Status**: Accepted (implemented — B-25-EXEC Phase 4)
- **Decision**: `dist/monorepo` composes with two rules on top of the Phase-3 composer, keeping it
  dumb (copy + marker substitution + overlay only):
  1. **Marker resolution order**: `src/stacks/monorepo/snippets/<rel>/<NAME>` if present (authored
     merged/sectioned content) → else the dotnet snippet then the angular snippet, **raw
     concatenation** (union semantics; one-sided snippets pass through).
  2. **Whole-file overlay**: union of `dotnet/files` + `angular/files` + `monorepo/files`;
     `monorepo/files` wins; a path present in **both** stack dirs with **no** monorepo override is
     a **build error** — no silent last-wins, every collision is an explicit authored decision
     (red-tested: 38/38 collisions reported before any content existed).
- **Rejected**: (a) qualified include-markers (`@stack:dotnet:NAME`) inside monorepo snippets —
  a templating feature without enough consumers (Leanness #8); (b) composer-inserted
  `### .NET`/`### Angular` headings — the composer cannot know prose vs YAML-frontmatter vs script
  context and would corrupt single-line snippets.
- **Measured facts** (2026-07-10): all **114** shared snippet pairs were divergent (zero identical),
  so union-by-default only stays derived where content is additive-safe — **5** markers remain
  derived (audit-trail skip ×2 = independent guard branches; `route-prompt.sh` sensitive-grep =
  self-contained `if…fi` lines; 2 one-sided). **111 authored monorepo snippets + 38 authored
  whole-file overrides** carry everything else. `dist/monorepo` = **148 files** (128 common +
  10+10 stack-only). **D4 token gate: monorepo CLAUDE.md = 27,589 chars ≈ 1.17× the larger
  single-stack (angular 23,638) — well under the ~1.5× fallback trigger; single root file stands.**
- **Trap recorded** (see LEARNINGS): the plan's "snippets are additive-safe" held for the `.sh`
  twin but NOT its `.ps1` twin — `$sensitive = $lc -match 'A'` concatenated with `$sensitive =
  $lc -match 'B'` lets the second assignment overwrite the first (a .NET-only keyword would be
  erased). `route-prompt.ps1/sensitive-regex` needed an authored `(-match A) -or (-match B)`
  monorepo snippet. Additive-safety must be judged **per twin**, never assumed to transfer.
- **Accepted cost / discipline**: authored monorepo snippets/files are a third authoring surface
  for divergent content. Editing a stack snippet or stack whole-file that has a monorepo sibling
  requires reviewing that sibling in the same task (recorded in DEVELOPING.md). Core edits and the
  5 derived markers flow to all three dists automatically; CI dist-freshness still proves
  src→dist for all three.

## WSD-010 appendix (2026-07-11): B-27 mechanics retargeted to the monorepo

- **Date**: 2026-07-11
- **Status**: Accepted (mechanics remap only — D1–D10 unchanged, still target **v0.27.0** in
  this repo)
- **Decision**: the B-27 team-wiki-memory design
  (`.claude/plans/2026-07-04-b27-wiki-memory-design.md`) predates the merge (WSD-012) and named
  its delivery mechanics in dual-repo terms — its own retarget banner flagged this and deferred
  the mapping to "merge Phase 5." Phase 5 is now done (WSD-016): `check-lockstep.ps1` is
  **retired**, and the workspace meta layer lives inside this repo. The remap:
  - **(a) Its proposed `check-lockstep.ps1` `$identicalFiles` additions (D9)** — `docs/wiki/
    INDEX.md` and `_template.md` were to be added to `check-lockstep`'s cross-repo identical-file
    set so drift between the two repos' wiki skeletons couldn't ship silently. There is no
    cross-repo identical-file set anymore: `docs/wiki/INDEX.md` and `_template.md` are authored
    **once**, under `src/core/docs/wiki/` (or the appropriate `src/stacks/<stack>/files/` tree if
    a stack ever needs a divergent skeleton — unlikely, since the wiki design is stack-agnostic).
    Parity across the three dists is now a **structural** property of the composer (one source,
    three copies) rather than a gated cross-repo diff. The gate that replaces D9's intent is each
    dist's `scripts/template-checks.ps1/.sh` plus the repo-root `scripts/validate-dist.ps1/.sh`
    (run per dist, ×3) — they must be extended to assert `docs/wiki/INDEX.md` and `_template.md`
    are present and schema-valid in every dist that ships them, the same role `check-lockstep`'s
    `$identicalFiles` used to play for cross-repo parity.
  - **(b) "Implement in dotnet → mirror to angular" (D9's touchpoint checklist, and every
    "both repos" instruction elsewhere in the design doc)** — becomes single-source edits under
    `src/core` (CLAUDE.md companion preamble, §5 checklist line, Common Tasks skill list, "What
    We've Learned" boundary sentence; `docs-sync-check.ps1/.sh`'s wiki-check child call;
    `ci-integration.md` Leg 1 text) that flow to all three dists on rebuild — invariant #1's
    single-source-composition discipline (root `CLAUDE.md` §Meta-invariants #1), not a
    dual-repo mirror step. Where the design's touchpoints are stack-specific (`bootstrap.md`,
    `adopt.md`, `docs-sync.md`, `generate-copilot.md`, the `remember-for-team` skill, and the
    `.ps1`/`.sh` `wiki-check` twins themselves — none of these differ by stack in the original
    design, so they land in `src/core`), they land in `src/core` too; the **only** place a
    monorepo-sibling review applies (WSD-015) is if a future edit to one of these touchpoints
    needs stack-divergent snippet content — none of D1–D10's content is currently stack-divergent,
    so no monorepo snippet authoring is anticipated for B-27 itself.
  - **(c) Its install ownership matrix (D8)** — `docs/wiki/INDEX.md` copy-if-absent in all modes,
    `_template.md` + `wiki-check.*` always-copy, `INDEX.md` joining the adoption-signal set —
    applies unchanged **per dist mode**. `install.ps1`/`install.sh` at the repo root are thin
    stack-detecting wrappers that delegate to the chosen dist's own installer
    (`dist/<stack>/scripts/install.*`, byte-frozen per dist); D8's ownership rules are implemented
    once in `src/core/scripts/install.ps1/.sh` (the composer copies them into every dist
    unchanged) and exercised identically by whichever dist installer runs. The **`dist/monorepo`
    installer carries the union** — a mixed consumer repo gets one wiki skeleton regardless of
    which stack(s) triggered adoption, so D8's matrix needs no monorepo-specific variant.
  - **(d) Wiki-check validation twins (`wiki-check.ps1`/`.sh`, D2/D6/D10)** — ship from
    `src/core/scripts/` like every other composer-copied script, and are validated **per dist** by
    `scripts/validate-dist.ps1/.sh <dist>` (bash -n / PS-AST parse, then the red-tested behavioral
    fixtures in `WikiCheck.Tests.ps1`, which each dist carries under its own `tests/hooks/` and
    runs via that dist's `Invoke-HookTests.ps1` — the composer/gate framing's substitute for
    D10's "run both twins on the same temp fixtures, assert identical verdicts" now applied
    identically inside each of the three dists, not across two repos).
  - **D1–D10 themselves are unchanged** and still target **v0.27.0** in this repo — implement
    from the design doc as written; this appendix only remaps *how the work lands*, not *what*
    D1–D10 specify. The doc's own retarget banner (top of file) and WSD-012's retarget note
    (above) both anticipated exactly this deferred mapping.
- **Context**: written at Phase 5 (WSD-016) completion, so the remap could be checked against the
  actual retired/replacement mechanics rather than predicted ones — `check-lockstep.ps1` and its
  test suite are confirmed gone (see WSD-016), and the composer/`validate-dist` gates that replace
  its job are confirmed live (WSD-012 Phase 3/4 execution deltas, `6acb8e5`/`7f621c6`).
- **Consequences**: the B-27 implementing session should read D1–D10 from the design doc plus this
  appendix for mechanics — not re-derive delivery mechanics from the (now historical) two-repo
  framing in the design doc's own D7–D9 prose. Any further delta discovered during B-27
  implementation gets appended here, not silently applied, per the design doc's own instruction.

## WSD-016: B-25-EXEC Phase 5 — meta-layer move (D7) + single-repo release retarget

- **Date**: 2026-07-11
- **Status**: Accepted (decided and executed)
- **Decision**: execute WSD-012's **D7** — move the workspace governance layer *into* this repo
  and retire the two-repo maintainer tooling it replaces:
  - **Moved in:** `CLAUDE.md`/`AGENTS.md`/`DEVELOPING.md` at repo root, rewritten for single-repo
    reality (invariant #1 "dual-repo lockstep" → "single-source composition," per the new root
    `CLAUDE.md` §Meta-invariants); the `bom-fix` hook twins + their meta test suite (now under
    this repo's `.claude/hooks/...`, scope narrowed — see below); the old workspace root's
    `BACKLOG.md`, `workspace-decisions.md` (this file), and `ci-handover.md` → this repo's
    `docs/`; the old workspace root's `.claude/plans/` — including the **locked** B-21
    (reviewer-profile, WSD-013), B-22 (headless `/adopt`, WSD-014), and B-27 (wiki memory,
    WSD-010) design specs — moved here too, beyond D7's originally-named file list, so those
    signed-off designs survive the old workspace's eventual retirement instead of being stranded
    in a repo nobody reads anymore; the delta is recorded here since D7's list didn't name it.
    The old workspace root's meta `LEARNINGS.md` → this repo's root `LEARNINGS.md` (distinct
    from the shipped `src/core/LEARNINGS.md`, unchanged).
  - **Retired:** `check-lockstep.ps1` and `CheckLockstep.Tests.ps1`. Their job — catching
    cross-repo drift between `ai-tech-lead-dotnet` and `ai-tech-lead-angular` — no longer exists
    as a category of bug once there is one authoring source (`src/`); what remains of it is
    covered structurally by the composer (one source → three dists, no drift possible by
    construction) and by `scripts/validate-dist.ps1/.sh` (per-dist content/schema checks) plus
    `scripts/fidelity-check.ps1/.sh` (the migration-era byte-fidelity gate, see below).
  - **`bom-fix` hook retargeted.** Its scope regex moved from matching
    `ai-tech-lead-(dotnet|angular)` path segments to matching anywhere under `ai-tech-lead/` (this
    repo, full stop — there is no longer a "which of two repos am I in" branch). The `.ps1`/`.sh`
    twins and their existing behavior are otherwise unchanged. `WorkspaceBom.Tests` now sweeps the
    **whole repo** (previously it swept two repo roots by name) — a strictly broader check, since
    `src/`, `dist/`, `scripts/`, and `.claude/` are all now one tree. `MetaHooks.Tests` gained
    twin-agreement cases that plant a file path shaped like the old `ai-tech-lead-dotnet/…` /
    `ai-tech-lead-angular/…` layout and assert the hook does **not** fire on it — the regression
    test for "the hook silently stops doing its job because its scope regex still points at a
    retired repo-name pattern," proving the retarget rather than just asserting the new regex
    compiles.
  - **`release.ps1` retargeted to one repo.** Precondition changed from "both repos' CHANGELOG
    heads agree" to **this repo's root `CHANGELOG.md` head entry** being the thing that gates a
    release. On a clean release it: stamps `src/core/CLAUDE.md`'s version header plus the three
    `src/stacks/{dotnet,angular,monorepo}/files/.claude/framework-version.json` files (was: two
    repos' `CLAUDE.md` + `framework-version.json` pairs); rebuilds all three dists
    (`scripts/build.ps1 <dist>` ×3, was: nothing — the legacy repos shipped source directly); runs
    the full gate set — dist freshness (`git status --porcelain dist/` empty after rebuild),
    `validate-dist.ps1` ×3, each dist's hook suite (`Invoke-HookTests.ps1` ×3), and this repo's
    meta test suite (was: `CheckLockstep.ps1` + two repos' `template-checks` + two hook suites);
    commits and pushes **one** repo (was: two, in the same task). **`fidelity-check.ps1/.sh` is
    deliberately NOT a release gate** — it is a migration-era gate pinned to the `freeze-v0.25.5`
    tags on the now-frozen legacy repos, proving zero-behaviour-change during the merge itself, not
    an ongoing property of future releases. The **v0.26.0 release (Phase 6)** must consciously
    retire or re-baseline the CI fidelity legs in the same change — together with the
    already-queued `actions/checkout` v4→v5 bump in the shipped workflows (recorded in the prior
    B-25-EXEC follow-up note) — rather than carrying a gate that compares against a tag that will
    soon represent an archived, no-longer-maintained pair of repos.
  - **Shipped dist content untouched in Phase 5.** Fidelity stays 138/138 per stack against the
    `freeze-v0.25.5` baselines (WSD-012's Phase 0 tags) — this phase moved maintainer tooling and
    docs, not `src/` or `dist/` content. Root `CHANGELOG.md` opens with an **Unreleased v0.26.0**
    entry (see the new file itself) describing the merge; it does not yet claim a shipped release.
    The old workspace root (`C:\temp\AIdrivenDev\`, one level up from this repo) keeps only a
    pointer stub plus `MERGE-MIGRATION-PLAN.md` until Phase 6 formally archives the two legacy
    repos.
- **Context**: WSD-012 D7 named the move but deferred its execution to "Phase 5"; this entry is
  that execution's record, per WSD-012's own instruction that "deltas discovered during execution
  get appended here, not silently applied." The `.claude/plans/` scope-widening (B-21/B-22/B-27
  specs, not just D7's named files) was the one delta found during execution — recorded above
  rather than silently applied.
- **Consequences**: this repo's root `CLAUDE.md` is now the sole authority for meta-development
  (superseding the old workspace root's `CLAUDE.md`, which becomes historical once Phase 6
  archives that root). `meta/BACKLOG.md` and this file are the live ADR/backlog surfaces going
  forward. B-27 implementation (WSD-010, target v0.27.0) reads its delivery mechanics from the
  appendix above, not from re-deriving them against the now-retired `check-lockstep.ps1`. Phase 6
  (validation → archive the two legacy repos → tag v0.26.0) remains the next and final phase of
  B-25-EXEC; it must not begin until this repo's gates (dist freshness, `validate-dist` ×3, hook
  suites ×3, meta suite) are all green against the Phase 5 state.

## WSD-017: B-32 context-footprint gate — design LOCKED (measure + freshness-gate the always-loaded surface)
- **Date**: 2026-07-11
- **Status**: Accepted (design locked; implementation post-merge — after Phase 6 / v0.26.0,
  before or with B-27; the doctor surfacing rides B-16)
- **Decision**: build `scripts/context-footprint.ps1/.sh` twins that deterministically measure
  what the framework injects into consumer sessions, per dist, against a committed baseline
  `docs/context-footprint.json` — a freshness-style gate in the check-lockstep/dist-freshness
  tradition. Manifest groups by *when the consumer pays*: `static.claude` (CLAUDE.md +
  skills/commands/agents frontmatter), `static.copilot` (AGENTS.md + copilot-instructions.md),
  `instructed` (FRAMEWORK-CONTEXT.md, docs/defaults.md, post-B-27 the wiki INDEX seed),
  `session` / `prompt` (session-start and route-prompt rendered against pinned deterministic
  fixtures — **both hook twins rendered, FAIL on output mismatch**, which doubles as the
  first rendered-rails content-parity gate), `ondemand` (info-only). FAIL = measured ≠ baseline
  or twin mismatch; WARN (never fail) = advisory ceilings, recorded in the JSON: `static.claude`
  > 40K chars single-stack / 48K monorepo, and monorepo CLAUDE.md ratio > 1.5× — the latter
  **absorbs WSD-015's one-off D4 token check** into a repeatable gate. Counting rule: LF-normalized
  UTF-8 bytes, tokens ≈ chars÷4 (no tokenizer dependency); canonical hand-rolled JSON emission
  (byte-identical across PS 5.1/pwsh/bash). Wiring: CI cross-OS legs run `-Check` (= twin proof);
  `release.ps1` runs `-Update` (the version stamp lands after PRs were already gated, so the
  release delta is the stamp alone). B-27's `wiki-check` must reuse the counting rule.
  Full spec: `.claude/plans/2026-07-11-b32-context-footprint-gate-design.md` (rev-2,
  adversarially critiqued — LOCK WITH AMENDMENTS; 2 HIGH + 5 MEDIUM + 5 LOW all folded; both
  HIGHs independently re-verified: monorepo `static.claude` = 41,443 chars already exceeded a
  naive shared 40K ceiling, and shipped `.github/copilot-instructions.md` was missing from the
  draft manifest).
- **Context**: the maintainer raised rising consumer token-cost consciousness (2026-07-11) —
  the literal trigger recorded in B-26 ("re-measure if context budgets tighten"). Triage found
  model-tiering already decided (WSD-011, static per-agent tiering; evidence gap = B-29) and no
  measurement machinery anywhere: current static per-prompt overhead measured at ≈ 8.5–10.4K
  tok on Claude Code and ≈ 6.0–7.6K on Copilot, previously invisible.
- **Alternatives rejected**: fold into `validate-dist` (stateless per-dist validator vs a
  stateful cross-dist gate); measurement-only report at release (recreates the B-26 advisory
  rot); real tokenizer (dependency, no decision-relevant precision); hard-fail ceilings
  (threshold-fiddling risk — the deterministic FAIL already prevents silence); measuring only
  the `.sh` hook twins (consumers' settings.json wires the `.ps1` twins — they'd be ungated).
- **Also decided in the same pass**: NO standalone "output/report leanness" backlog item — it
  conflicts with the reviewer profile (WSD-013: reports must stay explanatory for engineers
  with limited AI understanding); folded instead as a verbosity-sanity line on B-21's
  implementation checklist. B-29 amended: it is the enabler for safely *extending* WSD-011
  tiering, and gains the "should `.github/agents/*.agent.md` pin GitHub's documented `model:`
  field" question. The gate targets growth/churn **visibility**, not size minimization —
  B-26/WSD-011's salience-over-bytes trade stands (design D8 records this so a future session
  doesn't compress the rails against the ceiling).
- **Implementation deltas (2026-07-15, appended at B-32 execution)**:
  1. **Baseline path is `meta/context-footprint.json`, not `docs/context-footprint.json`** — the
     spec predates v0.26.1 (WSD-019), which moved the maintainer layer to `meta/` and removed the
     root `docs/` by decision (root `CLAUDE.md` records why). Same role, new namespace; nothing
     else about D3–D5 changes.
  2. **The twin-render fixtures found their first real defect before the gate even existed:** the
     shipped `.ps1` twins of session-start and route-prompt rendered ASCII-flattened rails
     (`WARNING:`/`--`) where the `.sh` twins emit the designed `⚠`/`—`/`→` text — exactly the M5
     gap the spec predicted. Decision: `.sh` content is canonical; the `.ps1` twins (core + the
     route-prompt stack snippets) were aligned to byte-identical rendered output and shipped as
     **v0.26.5** in the same release that lands the gate (the gate's baseline requires agreeing
     twins to exist). guard/audit-trail were deliberately left out of scope → **B-34**.
  3. **Two PowerShell correctness traps caught only by independent verification, not by the
     implementer's own testing** (the implementer's sandbox could not execute bash, so it could
     never run its own `.ps1` end-to-end against real dist content): (a) `Measure-Object
     -Property <name> -Sum/-Maximum` silently returns `$null`/0 on `[ordered]` hashtable items —
     unlike `Where-Object -Property`/`Sort-Object -Property`, which *do* resolve dictionary keys
     as pseudo-properties, `Measure-Object`'s binder does not, and it fails silently rather than
     erroring. Fixed by summing manually. (b) the classic `return ,$array` single-object-pipeline
     idiom (used to stop PowerShell from unrolling a 0/1-element array on return) breaks if the
     *caller* also wraps the call in `@(...)` — the double-wrap nests the real array one level
     deep, and every downstream `Where-Object`/lookup against it silently returns nothing. Both
     bugs produced no error at the point of failure (silent 0/`$null`, or a crash several
     unrelated lines downstream) — this is why the design's per-line red-test matrix and an
     independent verifier re-running the twins against real content (not trusting the
     implementer's own "it works" claim) both earned their cost here. **Status: SHIPPED v0.26.5
     (2026-07-15).**

- **Status (2026-07-12): Phase 6 COMPLETE — the merge is fully executed and shipped.** The
  reversible validation (below) ran green; then, on maintainer go-ahead (dotnet 8.0.422 + ng 21
  installed mid-session, enabling the real-toolchain re-run), the irreversible tail executed:
  v0.26.0 released via `release.ps1` (commit `ad717c7`, 11/11 gates green — compose ×3 +
  validate-dist ×3 + hook suites ×3 + meta), `master` + annotated tag **v0.26.0** pushed, pointer
  READMEs committed to both legacy repos (dotnet `f018085`, angular `433f258`, each atop its
  `freeze-v0.25.5` SHA), and **both legacy GitHub repos archived** (`isArchived:true`). The
  release folded the two deliberate shipped-content changes: `actions/checkout` v4→v5 in the
  shipped workflows, and retirement of the CI strict-fidelity legs (freeze tags are no longer the
  baseline). Acceptance criteria 1–6 met. **The abort rule never fired.**
- **Real-toolchain validation re-run (2026-07-12, superseding the simulated pass):** scaffolded a
  real `dotnet new webapi`, real `ng new`, and a real mixed repo; installed all three dists via
  root-installer auto-detect; `docs-sync-check` deterministic framework checks pass in each real
  install (only the by-design bootstrap-pending signals remain until `/bootstrap`); monorepo
  `route-prompt` fires the security overlay for a .NET money keyword AND an Angular sanitisation
  keyword (both twins); the mixed repo's per-stack exemplars are directory-and-extension disjoint
  (`.cs` under `api/`, `.ts` under `web/`) so bootstrap subsection routing cannot cross-contaminate.
- **Evals decision:** the live eval run was **skipped for v0.26.0** — `run_evals.py` is hardcoded
  to the Anthropic Messages API (needs `ANTHROPIC_API_KEY`; Copilot CLI is not a drop-in — that
  is a shipped-file rewrite = B-23), evals are **not** a release gate (WSD-016) and never have
  been, and the merge is proven zero-behaviour-change so there is nothing new for them to catch.
  Structurally validated only (monorepo `cases.yaml` = 14-case union of 7+7, parses). Run later
  with a key if desired (feeds B-23).
- **Original reversible-portion evidence (2026-07-12, first pass):** the reversible half of Phase 6
  (`MERGE-MIGRATION-PLAN.md` §5) ran green first on the maintainer box (pwsh 7.6.3) before the
  toolchains were installed.
- **Deterministic gates re-run GREEN (foundation):** compose ×3 with `dist/` freshness empty;
  `validate-dist` ×3 exit 0; strict `fidelity-check` ×2 exit 0 (dist still byte-matches the
  freeze-v0.25.5 baseline, allowlist empty — nothing has drifted); hook suites ×3 (0 failures /
  7 files each, incl. TwinParity 40/40); meta suite (0 failures / 2 files).
- **Phase 6 item 1 — install each dist + `docs-sync-check` (evidence):** greenfield installs via
  the root `install.sh` into scratch targets exercised **all** stack-resolution paths —
  auto-detect dotnet (`*.sln`/`*.csproj`), auto-detect angular (`angular.json`), auto-detect
  **monorepo** (both markers → `template: monorepo`), explicit `--stack` override into an
  unmarked dir, update-stamp mode (re-install over an existing `.claude/framework-version.json`
  → "update" mode), and the no-detection error path (empty dir, no flag → exit 2, actionable
  message). Each install carried the correct file layout and did **not** leak `.template-repo`
  (consumer-CI marker correctly excluded). `docs-sync-check` on a *fresh* install exits 1 with
  **exactly** the two by-design "run /bootstrap" signals (`BOOTSTRAP_PENDING` in CLAUDE.md +
  `DETECTED_FRAMEWORK_PACKAGES_PENDING` in FRAMEWORK-CONTEXT.md) while **all deterministic
  framework checks pass** (stamp sync, verbatim CLAUDE.md↔AGENTS.md mirror ×5, BOM sweep, twin
  sweep, `bash -n`, skills mirror). Simulating a completed bootstrap (populating those two
  markers) makes `docs-sync-check` exit **0 — fully green ×3**. So "green ×3" holds once
  bootstrapped, and the merge-critical deterministic surface is green even pre-bootstrap.
- **Phase 6 item 2 — `route-prompt` security overlay in the monorepo install (evidence):** three
  fixtures piped to **both** twins in the installed `dist/monorepo` copy. A .NET money keyword
  (`ledger balance reconciliation`) and an Angular sanitisation keyword (`innerHTML`) **each
  independently fire the "Security-sensitive surface detected" overlay** (the additive `-or`
  merge — WSD-015's non-additive-`.ps1`-grep trap stays fixed in the shipped artifact); a benign
  prompt stays silent. `.sh` and `.ps1` twins agree on all three.
- **Phase 6 items 3 & 4 — maintainer-gated by design, structurally validated:** *(3) evals* —
  `run_evals.py` requires `anthropic` + `ANTHROPIC_API_KEY` and makes **billable live API calls**
  (B-23: human/cost-triggered, has never gated a release). Structural proof done without spend:
  monorepo `cases.yaml` = **14 cases = 7 (dotnet) + 7 (angular)**, the composer union; parses as
  valid YAML. The live run is the maintainer's to trigger. *(4) `/bootstrap` dry-run* — the
  command carries `disable-model-invocation` (**human-only by design**) and needs a real mixed
  codebase; not model-runnable. Static proof done: the monorepo `bootstrap.md` carries the
  mixed-repo instruction in full — "locate both stack roots", "skill discovery runs once not per
  stack", and the per-stack-subsection rule ("populate each subsection from that stack's own
  directories only … never a `.cs` path under the Angular subsection, or a `.ts` under .NET").
- **The irreversible tail — DONE 2026-07-12 (in order):**
  1. Evals — **skipped for v0.26.0** by decision (see the Evals-decision bullet above); not a gate,
     zero-behaviour-change, needs a key the harness has no Copilot substitute for. Feeds B-23.
  2. `/bootstrap` dry-run — the full interactive workflow is developer-gated (`disable-model-
     invocation` + human-input pauses + subagent passes); its one monorepo-specific behavior
     (per-stack exemplar routing) was validated dynamically against the real mixed repo instead.
  3. **Release-execution items — DONE:** bumped `actions/checkout` v4→v5 in the shipped workflows
     (flowed to all three dists; fidelity then failed on exactly those 3 files, confirming the
     deliberate change); retired the CI strict-fidelity legs in `.github/workflows/ci.yml`
     (freshness + validate-dist + hook suites remain); wrote the v0.26.0 CHANGELOG (root +
     3 shipped stack changelogs — the first `release.ps1` run REFUSED here, template-checks caught
     the un-stamped shipped changelogs, fixed then green); released via `release.ps1` → `ad717c7`.
  4. **DONE:** pushed `master` + annotated tag **v0.26.0** (`dcca7dd`); pointer READMEs pushed to
     both legacy repos (dotnet `f018085`, angular `433f258`); **both legacy repos archived on
     GitHub** (`isArchived:true`). B-25-EXEC is complete; B-27 (team wiki memory) is next as
     v0.27.0 in this repo.

---

## WSD-019: The meta/product boundary — sealed, namespaced, and made a machine check (v0.26.1)

**Context.** Invariant #6 ("don't-ship boundary") has existed since the meta layer was created, and
the framework's own `LEARNINGS.md` already says in plain words that *"instructions enforced only by
a model reading them are wishes; the fix was machine checks."* Both were true. Neither held. A
`no-meta-leak` sweep of the composed dists found **192 leaking lines** (81 dotnet / 83 angular /
28 monorepo), in two tiers — and the distinction matters, so state it honestly:

- **Tier 1 — actually installed into a consumer's repo: 22 lines** (measured on a real
  `install.sh` smoke run into a greenfield .NET target). Tracking ids in **live shipped code**:
  `post-write.{ps1,sh}` (all three stacks), `template-checks.{ps1,sh}` (which also named the
  maintainer-only `release.ps1` — a script that does not exist in a consumer repo),
  `build-architecture-html.ps1`, and four `tests/hooks/*.Tests.ps1`. This is unambiguous leakage.
- **Tier 2 — product-visible but not installed: ~170 lines**, almost all in
  `dist/dotnet/CHANGELOG.md` (66) and `dist/angular/CHANGELOG.md` (75), which were maintainer
  engineering logs (backlog ids, `WSD-nnn`, the "Fable-exit" codename, "lockstep with the .NET
  twin", links to the archived legacy repos, and a literal `_Maintainer-only (does not ship)_`
  note). The installer deliberately excludes `CHANGELOG.md`/`README.md` from the copy, so these
  never reach a consumer's working tree — but they *are* the published product surface that a
  team reads when evaluating, cloning, or installing the framework. Still a defect; a lesser one.

The gate scans `dist/` and therefore covers both tiers. Being precise about which is which is the
point: the framework's own enforcement-honesty doctrine forbids overclaiming a harm, and "192 lines
shipped into consumer repos" would have been an overclaim.

**Crucially, the merge did not cause this — it inherited it.** The legacy
`ai-tech-lead-dotnet/CHANGELOG.md` carries the identical 35 marker lines; the v0.25.5 fidelity
freeze copied them byte-for-byte, exactly as designed. The merge only made them visible by putting
the meta layer in the same tree.

**Decisions.**

- **D1 — the shipped changelog is the consumer's, not ours.** `src/stacks/*/files/CHANGELOG.md` is
  rewritten in the consumer's voice: what changed in *their* repo, what they must do. Every version
  heading is preserved (37/38/2 — unchanged), only the framing changed. This was safe to do because
  the full engineering history already lives, verbatim, in `meta/changelogs/legacy-*.md` — the
  shipped changelog was a near-duplicate of it. Our own engineering log is the **root**
  `CHANGELOG.md`; it may keep tracking ids, and it no longer calls itself "consumer-facing".
- **D2 — namespace the maintainer layer under `meta/`.** `LEARNINGS.md`, `BACKLOG.md`,
  `workspace-decisions.md`, `ci-handover.md`, `changelogs/` move there, and root `docs/` is
  **deleted outright** — that name belongs to the consumer (`dist/*/docs/`), and having both is what
  made a maintainer open `LEARNINGS.md` and find meta content where the product's empty template
  was expected. `CLAUDE.md`/`AGENTS.md`/`.claude/` **cannot** move (Claude Code loads them from the
  root), so for those the banner remains the only tie-breaker. Accepted asymmetry: structure where
  structure is possible, prose only where it isn't.
- **D3 — enforce it: `validate-dist` check 6, `no-meta-leak`.** Scans each composed dist against
  `scripts/meta-denylist.txt`. **Scanned against `dist/`, not `src/`**, because `dist/` is what a
  consumer actually receives — a `src/`-only scan would miss anything a snippet composes *into* a
  shipped file. CI already runs `validate-dist` per dist on both legs, so no `ci.yml` change.
- **D4 — the denylist targets ID forms, not vocabulary.** `\bB-[0-9]{2}[a-z]?\b` and
  `\bWSD-[0-9]{3}\b` are denied; the bare word **`BACKLOG` is deliberately allowed** (the product
  legitimately reads the *consumer's own* `BACKLOG.md` in `adopt.md` and the installers' adoption
  signals), and bare **`twin` is allowed** (the shipped `.ps1`/`.sh` twins are a real cross-platform
  feature consumer docs must be able to name). This is why the `ALLOW` list is **empty**: a gate with
  a large allowlist is a gate people learn to switch off. Prefer adding a narrow `ALLOW` over
  weakening a `DENY`.
- **D5 — one denylist file, read by both twins.** Not duplicated into the `.ps1` and `.sh` legs. The
  cautionary precedent is in `meta/LEARNINGS.md`: `docs-sync-check.ps1` counted with
  `Measure-Object -Line` while its `.sh` twin used `wc -l`, so the same check could pass on one
  surface and fail on the other. A shared data file makes that class structurally impossible.

**Evidence.** The gate was written **before** the cleanup and run against the dirty tree — it went
red on the real defect (81/83/28 lines, checks 1–5 still green), and both twins reported identical
counts. Building it first also caught a genuine twin asymmetry *in the gate itself*: check 5 invokes
the dist's own `template-checks.ps1`, which `Set-Location`s into the dist and never restores it, so
a relative path resolved after it broke on the PowerShell leg while the bash leg (subshell) was
unaffected — fixed by resolving paths up front. That is invariant #3 catching a bug in the code
written to enforce invariant #6.

**Version.** Shipped as **v0.26.1** — shipped *content* changes, no behavior change — which leaves
v0.27.0 free for B-27 (team wiki memory) as already planned.

---

## WSD-020: B-35 + B-36 designs locked — evidence-gated technology claims; testing-strategy completion (2026-07-15)

**Context.** First structured consumer feedback since v0.26.0: a dev team with a MongoDB backend
reports the framework "goes with EF Core… should have derived things from the codebase." A
same-day audit verified the mechanism (worst: `boy-scout-check` heuristic #3 fires on
MongoDB.Driver's `ToListAsync`-family methods and demands the EF-only `AsNoTracking()`; plus
unconditional defaults.md Data Access, `/bootstrap` A2's closed "EF Core / Dapper" detection
list, an ungated `add-entity` recipe, and a hardcoded copilot-instructions DbContext line —
MongoDB appears zero times in `src/`). The maintainer also asked whether the framework guides
per-item testing strategy and covers building a suite where none exists.

**Decision.** Two independent backlog items, designs locked in `.claude/plans/`:

- **B-35 (P2, `2026-07-15-b35-derive-dont-assume-design.md`).** Root cause framed as
  *technology claims are not evidence-gated*, not "missing Mongo support". Fix at that
  altitude: one core "derive, don't assume" rule; conditional defaults.md Data Access blocks;
  open A2 detection + a 3a no-unevidenced-technology synthesis guard; `add-entity` Step 0
  evidence gate + 3a persistence audit line; boy-scout heuristic #3 requires in-file EF markers
  (with a Mongo-shaped red fixture); genericized copilot-instructions line. **Rejected:** a
  MongoDB dist (wrong altitude — multiplies the surface the merge just shrank, fixes one
  technology, leaves the assumption mechanism); "just run /bootstrap" (the hook misfires
  regardless; defaults/skills are deliberately live pre-bootstrap; A2's closed list biases
  bootstrap's own output).
- **B-36 (P3, `2026-07-15-b36-testing-strategy-design.md`).** Audit first: per-item testing
  doctrine is substantially present (rails, Test shape heuristic, add-tests + characterization
  mode, leanness #11–16, test-critic), and the meta layer's definition-of-done is adequate —
  so the item adds only the verified gaps: `add-tests` **Suite bootstrap mode** for zero-test
  repos (developer framework checkpoint → minimal harness → CI wiring → risk-first initial
  tests → honest TECH_DEBT backfill entry), a Feature-rail pointer to the decision procedure,
  and bootstrap outputs (suite absence as primary finding; target-test-shape line in
  Conventions > Testing). **Rejected:** a new `bootstrap-tests` skill (would violate our own
  no-parallel-infrastructure doctrine) and pushing suite creation into `/bootstrap` (it's a
  re-runnable dev task with an interactive checkpoint, not one-time analysis).

**Sequencing.** B-35 is consumer-reported incorrect behavior on a supported configuration —
candidate to ship as a v0.26.x defect fix *before* B-27 (team wiki memory, still v0.27.0).
B-36 is independent and can ride the same or a later release. Both specs are written for a
Sonnet-class implementer: exact files, sibling/twin sweeps, acceptance criteria, and explicit
out-of-scope fences.

## WSD-021: B-40 SQL / data-warehouse guidance — design locked and shipped (2026-07-17)

**Context.** Maintainer request (2026-07-16): the framework covers no SQL at all, yet most
consumer solutions have a SQL backend; deep focus wanted on data warehouses — schema/layer
understanding (staging → warehouse → marts), load patterns (fact/dim loads, versioned runs),
slowly changing facts/dimensions, partitioning — with the framework acting as tech lead on
following existing patterns and never loading the same data twice. Verified greenfield: zero
matches for staging/dimension/fact/SCD/partition anywhere in `src/`.

**Decision (B-40, `2026-07-16-b40-sql-dw-guidance-design.md`, shipped v0.31.0).** Two
dotnet-stack skills — `map-warehouse` (discovery/report, perf-shaped, exempt from exemplar
pinning) and `add-warehouse-load` (instance-shaped `add-X` recipe, exemplar-pinned) — plus
DW-aware `/bootstrap` A2 detection (two-tier evidence: SQL-repo artifacts AND ≥2 warehouse
signals grepped inside SQL artifacts only), a three-way Phase 3a keep/delete rule
(DW / SQL-only / neither), and two evidence-keyed defaults.md blocks (raw SQL/procs;
data-warehouse). All content B-35-consistent: Step-0 STOP gates, T-SQL as evidence-gated
illustration only, derive-from-siblings with ask-when-greenfield. Ships to dotnet + monorepo
only. **Rejected:** a data-warehouse dist (wrong altitude — same reasoning as B-35's rejected
MongoDB dist); folding discovery into `/bootstrap` (re-runnable dev task, same reasoning that
rejected `bootstrap-tests` in WSD-020); one mega-skill (different triggers, higher context cost).

**Process note.** The implementation plan was adversarially reviewed before execution; 11
findings folded in, including three that would have failed gates or shipped drift: the angular
shipped-CHANGELOG version-stamp cross-check (release would refuse), hand-editing the generated
`architecture.html` (docs-sync sha drift), and whole-tree Step-0 greps false-positive on xUnit
`FactAttribute`/prose (gate now requires SQL-tier AND ≥2 DW signals scoped to SQL artifacts).

## WSD-022: B-49 quarterly live-fire drill — design locked (2026-07-17)

**Context.** Maintainer request (2026-07-17, Fable strategic review session): a periodic test —
install the framework into a real repo (an open-source one), verify things work as expected, and
run evals around value-add, with Claude reminding quarterly. This is the recurring execution
vehicle the strategic backlog lacked: B-41 defines behavioral scenarios and B-43 defines host
recertification, but nothing made either *happen* on a calendar.

**Decision (B-49, `.claude/plans/2026-07-17-b49-live-fire-drill-design.md`, LOCKED).** Quarterly
maintainer-run drill: pinned real OSS targets (dotnet `dotnet-architecture/eShopOnWeb`, angular
`gothinkster/angular-realworld-example-app`, fallbacks + binding replacement criteria; SHAs
pinned per drill), full drill on ONE rotating stack per quarter + monorepo composed-fixture
install-smoke (never A/B — a composed fixture is not a real monorepo). Three halves per drill:
(1) an 8-row binary behavior checklist C1–C8 (installer contract, agent-driven install commit,
guard secret-block on both surfaces, post-write feedback, timeboxed interactive /bootstrap with
B-21 outputs, route-prompt rail injection, /review catching ≥2/3 planted issues, docs-sync-check)
— any FAIL files a P1; (2) an A/B value-add protocol — same day/model/prompt, FRAMEWORK vs BARE
arms, frozen 5-dimension rubric (fabrication, convention adherence, test discipline,
verification evidence, leanness; T3 scored as planted-issues-caught), within-quarter deltas are
the controlled claim, cross-quarter trends directional only, a delta trending to 0 is B-44's
retirement signal; (3) the B-43 host-recertification canaries date-stamped into
`meta/host-certification.md` (VS Code agent-mode row mandatory in cycle 1 — the standing B-03
gap; missing seats produce `not certified` rows, never omissions). Safety protocol binding:
`git remote remove origin` at clone, fixture-shaped fake secrets only, artifacts in scratch.
Reporting template + degradation order (drop A/B tasks first, never checklist/recert) pinned in
the plan. A cloud reminder routine (`trig_01EL25XDM2pMDaFkRBSGjF1V`, 08:00 UTC 1 Jan/Apr/Jul/Oct)
reminds; it cannot and does not run the drill. Drill #0 (recommended within 2 weeks) freezes the
plan's Appendix: SHAs, T2 mutation patch, T3 planted diff, per-target R2 checks.

**Rejected:** running the drill inside the cloud routine (no local hosts/toolchains, no spend
control); using the maintainer's work repo (that is B-42's field-evidence job — would lose
pinned-SHA reproducibility); hard-gating releases on drill outcomes (stochastic must not block
deterministic); shipping the drill kit to consumers (invariant #6).

**Process note.** Design adversarially self-critiqued before locking; 8 findings folded
(bootstrap timebox, cross-quarter validity demotion, frozen-rubric scoring order, target-rot
handling, no-silent-canary-skips, push-hazard remote removal, T2 suite-green masking check,
fake-monorepo exclusion). Full list in the plan.
