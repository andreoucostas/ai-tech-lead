# B-232 B native read-recovery observation

Date: 2026-09-09. Current interpretation: **successful complete retrieval, an imperfect
summary, and an overconstrained acceptance test; no product change**. The historical
frozen acceptance did not pass. That does not establish failed comprehension or unsafe
adoption execution. Delivery B remains deferred pending a justified product decision.
This is neither adoption execution nor a framework-effectiveness comparison.

## Interpretation correction after user challenge

The user asked whether the harness caused the result, then what should change.
Root re-read the exact dispatch, native tool results and answer. The loader did not
truncate the recovered document: all source text reached the model. However our
acceptance contract required one recovery method and a detailed answer checklist
while the actual task requested a concise summary. The earlier conclusion was too
strong in treating those omissions as established comprehension defects.

Preserve the frozen scores and reviews below as history, with these qualifications:

- Contiguous ranges were unnecessary to the practical retrieval objective. The native
  full-read option achieved complete delivery; choosing it is a method mismatch, not
  a retrieval failure. The candidate's causal contribution remains unmeasured.
- A concise answer can omit details without misunderstanding them. Checklist omissions
  remain omissions, but this prompt did not explicitly require every scored subquestion.
  Do not infer an execution defect from those omissions.
- The requested numeric references were wrong. The tool supplied raw unnumbered text
  and the probe exposed only `view`; we did not separately calibrate convenient exact
  line lookup. This is an instrument limitation and a possible contributor, not proof
  that the setup caused invented citations. Wrong numbers remain wrong.
- The answer blurred adoption-branch completion and human merge/default-branch guard
  release. That is an inaccurate or overbroad explanation; its cause and effect on
  actual workflow execution were not measured.

The proportionate correction is to the record and future acceptance design. If a
separate observation later has enough decision value to justify execution, accept any
supported read whose actual content covers the complete required source, state each
scored question in the task, and supply verified numbered text or accept section names
plus exact supporting quotations. Score retrieval, answer accuracy, unavailable evidence
and actual execution separately. A matched comparison would be needed to attribute an
improvement to the candidate. This is prospective guidance, not a changed score for the
old run, a new harness, or authority for another paid attempt.

One independently inspectable source inconsistency remains: all three adoption
overviews say either archive-verification failure restores the marker, while Phase 7
first verifies with the marker present and creates the recovery copy only after PASS.
A small future wording correction should distinguish leaving the existing marker
untouched before bootstrap from restoring saved bytes after its removal. Record that
bounded source repair in B-232; it does not justify the proposed general paging rule.

## Contract, review and route

The user authorized the bounded next step with "ok let's do it". The concrete contract
is `.claude/plans/2026-09-09-b232-read-recovery-probe.md`, frozen at `a7a65d6`;
SHA-256 `7D35A01353D7EE5379203ECE0BC96AF2C0229C57CF1F17CC5EE1ECE65221655B`.
Its content was frozen before dispatch; the Git checkpoint was recorded afterward.
Public source baseline: v0.86.1, `e47b3b6b2cefaa94ce806b1e9293abefda797142`.

A separate Codex reviewer (`read_probe_critique`, no implementation participation)
formed a blind-first threat model and returned REVISE. Root adopted its four bounded
corrections: trigger the obligation for any task needing governing instructions,
describe 30 credits as a post-call threshold, isolate host configuration, and freeze
source-anchored semantic criteria. The reviewer then ACCEPTed the contract and,
separately, inspected and accepted the concrete launcher/configuration. Its runtime
model identifier is not exposed; no different-model-family review is claimed.

The external workspace held five public .NET distribution documents and a new Git
root. Four documents were exact released copies; the rules carrier added only the
candidate paragraph. No consumer code, marker, active script, active hooks or remote
was included. A separate Copilot home held only account-selection information and
explicit settings: all hooks disabled, IDE auto-connect disabled, memory disabled,
local-only custom agents, and trust limited to the fixture. Additional instruction,
provider and telemetry environment overrides were cleared in the child. No credential
values were copied or printed; existing OS credential-store authentication worked.

Native CLI 1.0.83, `claude-sonnet-5`, medium effort, default context, fresh session
`a089e6aa-1612-4bfa-8b31-f13614e86cb5`. Only `view` was exposed; shell/write/URL
tools were denied, builtin MCP and remote/export disabled, and normal path checks
retained with automatic temporary-directory access disabled. This is the bounded
public-document route, not proof of the headless adoption isolation contract.

## Root-observed execution

The session reported 20.034 seconds, process exit 0, no timeout, and three model
requests within the one prompt/session. The usage receipt reports 13,365,900,000
nano-AIU, equivalent to **13.3659 AI credits**. The configured threshold was 30;
local CLI help says a call can exceed that value before subsequent calls stop.
No resume or paid retry occurred. Both the event log and usage receipt were retained.
Before/after file inventories, lengths and SHA-256 hashes matched, including Git files.

| Observation | Result |
|---|---|
| Exact candidate in initial native system context, before first workflow view | Observed |
| Initial unbounded view returns a real size error marked tool-success | Observed: 39.3 KB message, no workflow content |
| Subsequent successful contiguous range reads | **Not observed: zero range calls** |
| Actual recovery | Second `view` used native `forceReadLargeFiles: true` |
| Returned source completeness | Exact complete source text: 40,008 characters, 395 lines |
| Fixture mutations or additional tool calls | None; exactly two `view` calls |
| Frozen semantic/source-reference acceptance | **MISS**, independently reviewed below |

The second tool result's UTF-8 SHA-256 equals the source file:
`EFF94917C98E58F7B01E8A25D8A0D0EA1595A326AB3FE66FC86DBA7887C9C9D6`.
The tool telemetry's model-facing length also reports 40,008. This establishes
delivery of the whole document in this run; it does not establish comprehension.
The unexpected native option is a successful alternative retrieval observation,
not a retrospective pass of the predeclared paging requirement.

Before dispatch, the observer rejected the retained real tool-success/size-error as
content (0/395 lines), rejected an omitted range (295/395), accepted exact complete
synthetic source ranges (395/395), and rejected changed slice content. These are
observer controls, not native behavioral evidence. After the unexpected whole-file
route, root separately compared the actual response with source, changed one response
character to observe equality fail, then restored equality. That post-run comparison
does not rewrite the frozen range measure or claim pre-dispatch calibration for this
new route.

## Original semantic adjudication (qualified above)

The answer correctly described several controls: default interactive versus explicit
headless mode, staging external merges for people, the three headless denials, the
legacy skill-tree stop, the frozen-plan helper, marker deletion timing, and the three
completion checks. However it did not satisfy the frozen required groups:

- It omitted the headless bootstrap requirement to leave candidate hazards unverified
  (source line 42), failed-inventory stop/protected live-path boundary (53–54), required
  object-with-entries plan (209–223), and legacy missing-digest cannot-verify distinction
  (308). A general reference to the override table is not an account of those facts.
- It did not state the required retention of both recovery files after failure or
  their removal only after success (332–340), nor the continued default-branch marker
  and guards until human merge (40). These are material recovery/completion omissions.
- Its blanket "any failure restores the marker" misses the pre-bootstrap distinction:
  that first failure leaves the marker in place; the recovery copy is created only
  after it passes (308–310). The headless overview at line 40 itself compresses this
  distinction, so this is not counted as a novel product contradiction caused by the
  candidate paragraph.
- Numeric citations were frequently wrong: default/headless trigger is line 17,
  not 15; restricted tools are line 23, not 21; marker lifecycle is line 40, not 33;
  dirty-tree preflight is line 50, not 87. Section names sometimes locate the correct
  passage but do not repair the requested inaccurate line references.
- "Headless never reaches this state" is overbroad. Source line 38 requires its
  Phase-8 commit on the adoption branch; final human merge and default-branch guard
  release are separate. The answer does correctly say it must not open/merge a PR.

No need to decide every borderline paraphrase: the clear material omissions and
missing specified recovery method already prevent acceptance. Do not infer that
the native full-read option bypassed permissions: it used the same read-only tool,
and the observed subject was public documentation inside the allowed fixture.

## Original decision and RCA (qualified above)

A second separate Codex reviewer (`read_probe_result_review`), uninvolved in the
contract or launcher, read the frozen contract/source and recorded a blind-first
failure model before inspecting execution evidence. It independently confirmed
initial carrier presence, exact full returned source, alternate read method, unchanged
inventories, route/receipt facts, and the semantic/reference misses. Its disposition
was **exercised but failed the frozen delivery contract; keep B deferred**. It inspected
the calibration receipt but did not rerun that generator. Its runtime model identifier
is not exposed; this is independent session review, not an orthogonal-family claim.

Keep v0.86.1 and the existing shipped rules. Do not add the candidate paragraph or
launch a second model attempt to obtain a passing result. The smaller current route
can retrieve the whole workflow, but this run shows that full text plus more prose
does not establish source-grounded comprehension. There is no evidence here to
justify phase splitting, a general runner, another parser gate or host engineering.

Parser gates could not catch an error-only tool response, a model choosing a different
read method, omitted lifecycle conditions or wrong citations. The existing instruction
already asks for evidence and uncertainty; repeating recovery wording did not establish
the complete named outcome. The same class exposes other long prose workflows and
any completion claim based on successful tool execution alone. Preserve those limits
in B-232; do not expand this delivery into a generic comprehension audit.

Raw evidence remains outside authoring Git at `C:/TEMP/b232-read-recovery-20260909/`:
launcher, isolated fixture/configuration, exact prompt/arguments, source anchors,
events, process/usage receipts, before/after inventories and calibrated observations.
The account-bearing raw configuration and full native session are not published.
No ABP adoption, application fix, RK1 task arm, consumer write or product release ran.

Retained raw SHA-256 identities:

- `events.jsonl`: `C4D39780A596FC89D2D341017B1618886F72D8B75B3CF27FB38003754150F44A`.
- `usage.json`: `F42EFABC4407A88BA6AEFE3C58CDC7E61C2CD2D1866921A4DF3B1D427A9F5BB5`.
- Both inventories: `5F88D70D20436344E4CF4E570EF0683E03DC86AF71F398B6B03EE10624CE82D9`.

Local authoring checks passed under direct native PS7 and PS5.1 (legacy host at CP437):
BacklogHygiene 10/0, RepositoryPrivacy 7/0, DocTruth 16/0 on each. No shipped source or
distribution changed, so no new version, installer verification or live release eval
is claimed. Normal outgoing checks and push CI apply to the evidence delivery.
